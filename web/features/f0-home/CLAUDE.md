# f0-home — 홈·가족리그

- 이 디렉터리의 `AGENTS.md`와 `CLAUDE.md`는 한 쌍이다. 둘 중 하나를 수정하면 같은 작업에서 짝 파일도 동일하게 수정하고 완료 전 비교한다.
- 이 폴더만 수정한다. 스펙: `docs/기능명세.md` §4.
- F10은 공개 컴포넌트를 import한다. 펫·주간 질문 UI는 만들지 않는다.
- 데이터는 `shared/store`와 `shared/data/seed`를 사용한다.
- 완료: 골든 패스 ① + 부모·자녀 뷰 + `npm run build`.
