# f3-reason — 매매 기록 폼

- 이 폴더만 수정하고 f2용 공개 컴포넌트를 export한다. 스펙: `docs/기능명세.md` F3.
- 매수는 근거+확신도 필수, 매도는 근거 필수, 메모는 선택이다.
- 저장은 `shared/store` 액션을 사용한다. 기록은 F9 입력이며 F10은 승인된 선제 신호만 쓴다.
- 시험·점수·정답처럼 표현하지 않는다.
- 완료: f2 흐름에서 기록 저장 + `npm run build`.
