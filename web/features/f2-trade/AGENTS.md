# f2-trade — 모의투자

- 이 디렉터리의 `AGENTS.md`와 `CLAUDE.md`는 한 쌍이다. 둘 중 하나를 수정하면 같은 작업에서 짝 파일도 동일하게 수정하고 완료 전 비교한다.
- 이 폴더만 수정한다. F2·F3 단일 스펙: `web/features/f2-trade/SPEC.md`.
- 기록 폼은 `features/f3-reason`, 시세는 `app/api/quote`만 사용한다.
- 체결·탐색 이벤트는 F9·F10이 쓰도록 `shared/store`에 기록한다.
- 완료: 골든 패스 ② + `npm run build`.
