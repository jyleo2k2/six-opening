# 키움 가족 모의투자 리그 — 공통 규칙

부모·자녀가 각자 모의투자하고 행동 기반 투자성향을 비교하는 데모 웹앱. 자녀용 챗봇은 F10 "키웅이".

- 작업 전에는 가장 가까운 가드와 그 가드가 가리키는 기능 `SPEC.md`만 우선 읽는다. 기능 지도는 `docs/기능명세.md`, 제품 목표·법무·레드라인은 `docs/영웅키움_기획_통합문서_v2.md`, UI는 `docs/디자인시스템.md`, 인프라·API는 `docs/기술스택.md`를 관련 작업일 때만 추가로 읽는다.
- 제품 결정은 통합문서 v2, 기능 구현 상세는 해당 기능 폴더의 `SPEC.md`가 단일 원본이다. 전역 제품 결정과 충돌하면 통합문서 v2를 따른다.
- 가장 가까운 가드가 소유 범위를 정한다. 범위 밖은 수정하지 않고 공용 변경은 `web/shared/AGENTS.md`를 따른다.
- 모든 디렉터리의 `AGENTS.md`·`CLAUDE.md`는 한 쌍이며 내용이 같아야 한다. 둘 중 하나를 수정하면 같은 작업에서 짝 파일도 동일하게 수정하고 완료 전 전체 쌍을 비교한다.
- LLM은 `gpt-5.6-luna`와 서버 전용 `openai` Responses API만 사용하고 `web/shared/llm` 필터를 거친다. 종목 추천·매매 시점·목표가·수익률 전망·훈계는 금지한다.
- 구 기획 문서를 복구·참조하지 않는다. 기획 변경은 통합문서 v2부터 반영하고 [사실]/[추론]/[가정] 표기를 유지한다.
- 요청 범위만 최소 수정하고 추측하지 않는다. 코드 변경은 `web/`의 `npm run build`와 해당 골든 패스로 검증한다.

## 하네스: 병렬 Git 세션 관리

**목표:** 6명의 Claude·Codex 작업을 한 작업·한 브랜치·한 worktree·한 PR로 분리하고, 같은 파일을 동시에 수정하는 충돌을 시작 전에 차단한다.

**트리거:** 코드·문서 수정, 새 브랜치·worktree, 병렬 작업, 작업 재개, PR 충돌·정리 요청은 `.agents/skills/git-session-manager/SKILL.md`(Codex) 또는 `.claude/skills/git-session-manager/SKILL.md`(Claude)를 먼저 따른다. 단순 조회·설명은 예외다.

- 루트 `main` worktree는 상태 확인과 새 세션 생성만 하는 관제용이다. 파일을 수정하지 않는다.
- 새 작업은 `node scripts/git-session-manager.mjs start`로 시작하고, 기존 규칙 worktree를 이어서 쓸 때는 수정 전에 `claim`한다.
- 한 작업은 브랜치·worktree·활성 claim·PR을 각각 하나만 사용한다. 병합한 브랜치를 다음 작업에 재사용하지 않는다.
- 브랜치 이름은 `{사용AI}/{작업자이름}/{작업명}`으로 정한다.
  - `사용AI`: 소문자 `claude` 또는 `codex`.
  - `작업자이름`: `이재용`, `이호연`, `김설빈`, `강소정`, `박혜준`, `김경렬` 중 하나.
  - `작업명`: 한글 낱말만 쓰고 여러 낱말은 하이픈(`-`)으로 연결한다. 형식은 `^[가-힣]+(?:-[가-힣]+)*$`이며 영문·숫자·공백은 금지한다.
  - 예: `codex/이재용/챗봇-안전게이트`, `claude/김설빈/주문화면-오류수정`.
- worktree 경로는 `../KDA육개장-worktrees/{사용AI}/{작업자이름}/{작업명}`으로 정하고 브랜치 세 구간과 정확히 일치시킨다. 경로는 `start` 명령이 출력하는 값을 그대로 쓴다.
- 작업 시작 전에 수정할 파일·폴더를 claim한다. 범위를 넓힐 때는 먼저 claim을 갱신하며, 다른 활성 claim이나 열린 PR과 겹치면 범위를 나누거나 기존 작업을 끝낸다.
- `AGENTS.md`·`CLAUDE.md`, `web/app`, `web/shared`, 공용 패키지 파일, 제품·기술 기준 문서는 공유 핫스팟이다. 동시에 한 작업만 수정한다. 루트 가드와 제품·기술 기준 문서는 이재용 세션만 수정한다.
- Git 브랜치·worktree·claim 생성과 해제는 세션 관리 명령의 잠금 안에서 한 번에 하나씩 처리한다. 별도 Git 코디네이터 에이전트는 두지 않는다.
- 첫 의미 있는 커밋을 push한 직후 Draft PR을 열고 PR base는 `main`으로 둔다. 본문에는 `session-policy` 워크플로가 검사하는 여섯 섹션을 제목 그대로 넣는다: `## 작업 정보`, `## 변경 요약`, `## 작업 범위`, `## 공유 핫스팟`, `## 계약 변경`, `## 검증`.
- 계약 변경은 `SPEC/타입 계약 → 생산자 구현 → 소비자 구현 → 통합 연결` 순으로 처리한다. 다른 작업자의 미완성 변경을 stash·reset·checkout하지 않는다.
- PR 병합 후 worktree가 깨끗한지 확인한 다음 claim을 release한다. 브랜치·worktree 삭제는 병합 여부와 `origin/main` 포함 여부를 다시 확인한 뒤 수행한다.
- 2026-08-12 이전에 이미 진행 중이던 브랜치와 PR은 이름만 예외로 유지한다. 새 기능 수정은 새 규칙 브랜치에서 시작한다.

## 하네스: 어린이 뉴스 역할 파이프라인 프로토타입

**목표:** 오늘 시황 또는 선정 51개 기업의 직접적인 중요 사건만 골라, 어려운 용어를 모두 풀어 쓴 어린이 뉴스 저장 후보를 만든다.

**트리거:** 뉴스 수집·선별·요약·용어 처리·독립 검수·저장 파이프라인을 다룰 때는 `.claude/skills/child-news-pipeline/SKILL.md`를 따른다.

- 역할은 관련성 선별자, 어린이용 편집자, 독립 출고 검수자로 분리한다.
- 편집자에게는 선별된 문장만 주고, 독립 검수자에게는 원문 전체와 실제 노출문만 준다.
- 한 단계라도 실패하면 DB 저장 대상이 아니며, 일상 기사로 목표 개수를 채우지 않는다.
- 수동 역할 에이전트 검증은 사용자가 명시적으로 요청한 경우에만 실행한다.

## 규칙 변경 이력

| 날짜 | 변경 내용 | 이유 |
|---|---|---|
| 2026-08-12 | 어린이 뉴스 3역할 파이프라인 프로토타입 추가 | 주체·중요성·초점·어려운 용어를 분리 검증해 일상 기사와 주변 사실 유입을 차단하기 위해 |
| 2026-08-12 | 브랜치와 worktree 이름을 `{사용AI}/{작업자이름}/{한글-작업명}`으로 통일 | 6인 동시 작업에서 담당자와 작업 범위를 명확히 구분하기 위해 |
| 2026-08-12 | 작업자 6명 고정, Node 기반 claim·충돌 차단 하네스 도입 | 문서 규칙을 실제 작업 시작·편집·커밋·PR 검사로 강제하기 위해 |
