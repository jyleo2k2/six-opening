# 30종목 시세 연동형 모의투자 기능 기획서

## 1. 문서 개요

- 문서 버전: 1.0
- 작성일: 2026-08-11
- 대상 기능: `F2. 모의투자` + 주문 과정에 포함되는 `F3. 질문식 매매 기록`
- 구현 환경: Next.js(App Router) + TypeScript 기반 웹앱
- 기준 문서: `docs/기획서.md` > `docs/기능명세.md` > `docs/디자인시스템.md` > `docs/기술스택.md`

### 1.1 작성 목적

프로젝트 루트에 구현된 키움 시세·차트 API와 30종목 목록을 활용해 사용자가 실제 국내주식 가격을 보며 가상자금으로 매수·매도하는 모의투자 기능을 정의한다. 시세와 차트는 키움 API에서 조회하고, 주문·체결·거래내역·보유현황·질문식 기록 등 서비스에서 발생한 사용자 데이터는 자체 DB에 영속 저장한다.

### 1.2 핵심 결정

- [사실] 제공 종목은 `kiwoom-stocks.mjs`에 등록된 국내주식 30종목으로 제한한다.
- [사실] 현재 루트 서버는 현재가 REST 조회, 실시간 체결 WebSocket, 분봉·일봉·주봉 차트 조회, SSE 갱신을 지원한다.
- [결정] 키움 API는 시세와 차트의 원천으로만 사용하며 실제 주식 주문은 전송하지 않는다.
- [결정] 모든 매수·매도는 서비스 내부의 가상계좌에서 시장가로 즉시 체결한다.
- [결정] 거래내역, 질문식 기록 및 행동 이벤트는 브라우저 저장소가 아닌 자체 DB에 보관한다.
- [결정] 질문식 기록은 주문 과정의 필수 단계이며 체결 데이터와 연결해 저장한다.
- [가정] 1차 구현은 사용자 인증이 완료되어 `user_id`를 신뢰할 수 있는 상태에서 시작한다.
- [확인 필요] 자체 DB 제품과 배포 환경은 구현 착수 전에 확정한다. 본 문서는 관계형 DB 기준의 논리 모델을 제시한다.

---

## 2. 서비스 목표와 제외 범위

### 2.1 목표

1. 사용자가 선정된 30종목의 현재가와 가격 흐름을 쉽게 확인한다.
2. 사용자가 실제 돈이 아닌 가상자금으로 매수·매도를 경험한다.
3. 주문 전에 선택한 이유와 확신도를 간단히 기록한다.
4. 체결 결과가 현금, 보유 수량, 평균 매수가, 평가손익에 정확히 반영된다.
5. 거래와 탐색 과정에서 생긴 데이터를 이후 F9 성향 스코어링의 입력으로 활용할 수 있게 저장한다.
6. 키움 API나 네트워크가 불안정해도 마지막 정상 시세와 데모용 폴백으로 핵심 흐름을 재현한다.

### 2.2 MVP 제외 범위

- 실제 증권계좌 연결 및 실제 주문 전송
- 지정가 주문의 실제 구현
- 호가창, 캔들차트, 보조지표, 기술적 분석
- 신용·미수·공매도·레버리지 상품
- 30종목 밖의 자유 종목 검색 및 거래
- 배당, 액면분할 등 권리 처리 자동화
- 종목 추천, 매매 시점, 목표가, 수익률 전망
- 사용자 간 주식 선물 또는 자산 이전

---

## 3. 제공 종목

`kiwoom-stocks.mjs`를 종목 코드와 이름의 단일 원천으로 사용한다.

| # | 종목명 | 종목코드 | # | 종목명 | 종목코드 |
|---:|---|---|---:|---|---|
| 1 | 한국전력 | 015760 | 16 | 현대차 | 005380 |
| 2 | 우리금융지주 | 316140 | 17 | 삼성생명 | 032830 |
| 3 | KT | 030200 | 18 | 삼양식품 | 003230 |
| 4 | 하나금융지주 | 086790 | 19 | SK스퀘어 | 402340 |
| 5 | 기아 | 000270 | 20 | HD현대중공업 | 329180 |
| 6 | DB손해보험 | 005830 | 21 | 대한항공 | 003490 |
| 7 | 현대글로비스 | 086280 | 22 | 현대로템 | 064350 |
| 8 | 신한지주 | 055550 | 23 | SK하이닉스 | 000660 |
| 9 | HD한국조선해양 | 009540 | 24 | 삼성중공업 | 010140 |
| 10 | KB금융 | 105560 | 25 | 삼성물산 | 028260 |
| 11 | HD현대 | 267250 | 26 | 한화에어로스페이스 | 012450 |
| 12 | 메리츠금융지주 | 138040 | 27 | HD현대일렉트릭 | 267260 |
| 13 | 현대모비스 | 012330 | 28 | HMM | 011200 |
| 14 | KT&G | 033780 | 29 | 크래프톤 | 259960 |
| 15 | 삼성화재 | 000810 | 30 | 삼성전자 | 005930 |

- 종목 노출 여부와 정렬 순서는 운영 데이터로 확장할 수 있지만, MVP에서는 위 파일의 목록과 순서를 따른다.
- 종목코드는 문자열 6자리로 처리해 앞자리 `0`이 사라지지 않게 한다.
- 종목명과 종목코드는 주문 시점의 스냅샷도 함께 저장해 목록 변경 후에도 과거 거래내역을 온전히 표시한다.

---

## 4. 사용자 경험

### 4.1 전체 흐름

```text
모의투자 홈
  → 30종목 탐색·검색
  → 종목 상세(현재가·차트·내 보유 현황)
  → 매수 또는 매도 선택
  → 수량 입력·예상 금액 확인
  → 질문식 기록
  → 서버 검증 및 가상 체결
  → 체결 완료
  → 포트폴리오·거래내역 반영
```

### 4.2 종목 탐색

#### 화면 정보

- 회사명, 종목코드
- 현재가
- 전일 대비 금액과 등락률
- 상승 `▲`·하락 `▼` 표시
- 마지막 시세 갱신 시각
- 내 보유 여부
- 검색창과 업종 필터

#### 동작

- 회사명 또는 종목코드로 30종목 안에서 검색한다.
- 종목을 누르면 상세 화면으로 이동한다.
- 실시간 SSE 연결 중에는 현재가와 등락 정보를 갱신한다.
- 시세가 없으면 가격을 임의로 0원 처리하지 않고 `시세를 불러오는 중이에요` 또는 오류 상태를 표시한다.

### 4.3 종목 상세

#### 화면 정보

- 현재가, 전일 대비, 등락률, 시가·고가·저가
- 마지막 갱신 시각과 데이터 상태
- 기간별 선차트
- 내 보유 수량, 평균 매수가, 평가금액, 평가손익률
- 아이 눈높이 회사 설명 카드
- 매수·매도 버튼

#### 차트 기간

- `오늘`: 분봉 데이터
- `일별`: 일봉 데이터
- `주별`: 주봉 데이터

화면 문구는 어린이도 이해할 수 있게 표시하되, API의 실제 조회 단위와 UI 라벨은 구현 시 일치시킨다. 차트는 가격선 중심으로 제공하고 투자 판단을 유도하는 예측선이나 신호는 표시하지 않는다.

### 4.4 매수

#### 입력 및 표시

- 주문 종목
- 기준 현재가
- 매수 수량
- 예상 체결금액
- 주문 전 가상 현금
- 주문 후 예상 가상 현금
- 주문 후 해당 종목의 예상 자산 비중

#### 검증

- 수량은 1주 이상의 정수만 허용한다.
- 예상 체결금액이 가용 현금보다 크면 체결하지 않는다.
- 등록된 30종목인지 서버에서 다시 검증한다.
- 클라이언트가 보낸 가격을 신뢰하지 않고 서버가 관리하는 최신 시세로 체결한다.
- 동일 요청의 중복 체결을 막기 위해 주문 요청 키를 사용한다.

### 4.5 매도

#### 입력 및 표시

- 보유 수량과 평균 매수가
- 기준 현재가
- 매도 수량
- 예상 체결금액
- 예상 실현손익

#### 검증

- 수량은 1주 이상의 정수만 허용한다.
- 보유 수량을 초과하면 체결하지 않는다.
- 사용자의 최신 보유 수량을 DB에서 잠근 상태로 다시 확인한다.
- 동일 종목 매도 후 30분 재매수 제한은 MVP 화면에 안내하고, 적용 여부는 운영 정책 확정 후 서버 규칙으로 활성화한다.

### 4.6 질문식 매매 기록

주문 확인 뒤, 최종 체결 직전에 표시한다.

#### 매수 기록

- 매수 근거 1개 필수
  - 뉴스에서 봤어 (`news`)
  - 차트가 좋아 보여 (`chart`)
  - 친구·가족이 추천했어 (`recommendation`)
  - 이 회사나 제품을 잘 알아 (`knowledge`)
  - 그냥 느낌이 좋아 (`feeling`)
- 확신도 1개 필수: 25 / 50 / 75 / 100
- 한 줄 생각 선택 입력
- 화면에는 `정답은 없어!`를 함께 표시한다.

#### 매도 기록

- 매도 근거 1개 필수
  - 목표한 만큼 올랐어 (`target_reached`)
  - 더 떨어질까 봐 걱정돼 (`loss_concern`)
  - 다른 종목이 더 좋아 보여 (`alternative`)
  - 그냥 불안해 (`anxiety`)
  - 기타 (`other`)
- 한 줄 생각 선택 입력
- 매도에는 확신도를 입력하지 않는다.

#### 저장 원칙

- 질문식 기록은 체결된 거래와 1:1로 추적할 수 있어야 한다.
- 질문 답변을 클라이언트에서만 보관하지 않는다.
- 한 줄 생각에는 길이 제한을 적용하고 HTML·스크립트를 저장하거나 렌더링하지 않는다.
- 질문 답변은 F9 성향 분석 입력이지만 실력 점수나 등급으로 노출하지 않는다.

### 4.7 체결 완료

- 매수 또는 매도 완료 여부
- 체결 종목, 수량, 가격, 총 체결금액
- 체결 시각
- 매수 후 잔액 또는 매도 후 잔액
- `기록 완료! 키웅이가 기억해둘게` 안내
- 포트폴리오 보기와 거래내역 보기 버튼

### 4.8 포트폴리오

- 가상 현금
- 주식 평가금액
- 총자산
- 누적 수익금 및 수익률
- 종목별 보유 수량, 평균 매수가, 현재가, 평가손익
- 보유하지 않은 종목은 포트폴리오에서 제외한다.
- 총자산은 `가상 현금 + 모든 보유 종목의 현재 평가금액`으로 계산한다.

### 4.9 거래내역

- 매수·매도 구분
- 종목명과 종목코드
- 체결 수량과 체결 가격
- 총 체결금액
- 실현손익(매도)
- 체결 시각
- 사용자가 남긴 근거와 한 줄 생각
- 최신 거래 순으로 표시한다.

---

## 5. 시세·차트 연동 기획

### 5.1 기존 구현 자산

| 내부 경로 | 역할 | 현재 구현 |
|---|---|---|
| `GET /api/stocks` | 30종목 목록과 캐시된 현재가 | `kiwoom-30-stocks-dashboard.mjs` |
| `GET /api/quote?code={종목코드}` | 단일 종목 현재가 조회 | 키움 `ka10001` |
| `GET /api/chart?code={종목코드}&period={기간}` | 단일 종목 차트 조회 | 키움 `ka10080`·`ka10081`·`ka10082` |
| `GET /api/events` | 실시간 시세 갱신 | 키움 WebSocket `0B` → 서비스 SSE |

### 5.2 연동 원칙

- 키움 앱키와 시크릿키는 서버 환경변수에만 둔다.
- 브라우저가 키움 REST API나 WebSocket에 직접 접속하지 않는다.
- 웹앱은 서비스 내부 API만 호출한다.
- 현재가와 차트 응답은 공통 형식으로 정규화한 뒤 화면에 전달한다.
- 실시간 연결이 끊기면 자동 재연결하고 30종목을 다시 구독한다.
- 시세 응답에는 `updatedAt`과 `source`를 포함해 최신성과 출처를 판단한다.
- API 실패 시 마지막 정상 응답을 폴백 캐시에서 제공하되, 화면과 체결 기록에 캐시 사용 여부를 남긴다.
- [확인 필요] 키움 시세의 다중 사용자 재배포와 가상 체결 이용 범위는 외부 공개 전에 서면 확인한다.

### 5.3 차트 캐시

현재 구현값을 1차 기준으로 사용한다.

| 구분 | 최대 포인트 | 캐시 시간 |
|---|---:|---:|
| 분봉 | 1,170 | 30초 |
| 일봉 | 500 | 30분 |
| 주봉 | 260 | 30분 |

### 5.4 체결 가격 정책

- [결정] MVP의 가상 체결가는 서버가 보유한 해당 종목의 최신 현재가다.
- [결정] 주문 요청에 포함된 화면 표시 가격은 참고값일 뿐 체결 기준으로 신뢰하지 않는다.
- [결정] 실제 키움 주문 API는 호출하지 않는다.
- [가정] 발표 데모에서는 장외 시간에도 마지막 정상 가격으로 가상 체결할 수 있으며 `장 마감 가격 기준 모의체결`임을 표시한다.
- [확인 필요] 운영 환경의 시세 노후 기준과 장중 거래 차단 기준은 키움 응답 특성을 측정한 뒤 수치로 확정한다.

---

## 6. 자체 DB 기획

### 6.1 저장 범위

#### 외부 API에서 읽고 캐시하는 데이터

- 30종목 기본 정보
- 현재가와 전일 대비 정보
- 분봉·일봉·주봉 차트
- 실시간 시세의 마지막 수신 시각

#### 자체 DB에 영구 저장하는 데이터

- 사용자와 가족 리그 소속
- 가상계좌와 초기 가상자금
- 주문 요청과 처리 상태
- 실제 가상 체결 결과
- 현금 증감 원장
- 종목별 보유 수량과 평균 매수가
- 매수·매도 질문식 기록
- 거래 및 포트폴리오 스냅샷
- 종목 탐색과 주문 화면 행동 이벤트
- 중복 요청 방지 키와 감사 로그

### 6.2 논리 데이터 모델

| 테이블 | 핵심 필드 | 용도 |
|---|---|---|
| `users` | `id`, `family_id`, `role`, `display_name`, `created_at` | 부모·자녀 사용자 |
| `virtual_accounts` | `id`, `user_id`, `initial_cash`, `cash_balance`, `version` | 사용자별 가상계좌 |
| `stocks` | `code`, `name`, `is_active`, `display_order` | 30종목 카탈로그 |
| `orders` | `id`, `user_id`, `stock_code`, `side`, `quantity`, `status`, `idempotency_key`, `requested_at` | 매수·매도 요청과 상태 |
| `trades` | `id`, `order_id`, `stock_code`, `stock_name_snapshot`, `side`, `quantity`, `price`, `gross_amount`, `realized_pnl`, `quote_time`, `quote_source`, `executed_at` | 가상 체결 원본 기록 |
| `trade_reasons` | `id`, `trade_id`, `reason_type`, `confidence`, `note`, `created_at` | 질문식 매매 기록 |
| `positions` | `account_id`, `stock_code`, `quantity`, `average_price`, `updated_at` | 현재 보유 현황 |
| `cash_ledger` | `id`, `account_id`, `trade_id`, `entry_type`, `amount`, `balance_after`, `created_at` | 가상 현금 불변 원장 |
| `portfolio_snapshots` | `id`, `account_id`, `cash`, `market_value`, `total_asset`, `captured_at` | 자산 변화 및 F9 입력 |
| `behavior_events` | `id`, `user_id`, `session_id`, `event_type`, `stock_code`, `metadata`, `occurred_at` | 체류·조회·수량 수정 등 행동 데이터 |
| `audit_logs` | `id`, `actor_id`, `action`, `target_type`, `target_id`, `metadata`, `created_at` | 오류 조사 및 주요 변경 추적 |

### 6.3 제약 조건

- `orders.idempotency_key`는 사용자 범위에서 유일해야 한다.
- `trade_reasons.trade_id`는 유일해야 하며 체결 한 건에 기록 한 건만 연결한다.
- 매수의 `confidence`는 25·50·75·100만 허용한다.
- 매도의 `confidence`는 비워 둔다.
- 수량, 가격, 금액은 음수가 될 수 없다.
- 보유 수량과 현금 잔액은 음수가 될 수 없다.
- 금액 계산에는 부동소수점 타입을 사용하지 않고 정수 원화 또는 고정 소수점 타입을 사용한다.
- 과거 거래의 종목명·체결가·질문 기록은 현재 종목 정보가 변경되어도 수정하지 않는다.

### 6.4 체결 트랜잭션

```text
1. 사용자·가상계좌 확인
2. idempotency_key 중복 확인
3. 30종목 거래 가능 여부 확인
4. 서버 최신 시세와 갱신 시각 확인
5. 질문식 기록 유효성 확인
6. 가상계좌 및 보유 포지션 행 잠금
7. 잔액 또는 보유 수량 검증
8. orders 생성·상태 변경
9. trades 생성
10. positions 갱신
11. cash_ledger 추가 및 virtual_accounts 잔액 갱신
12. trade_reasons 생성
13. 전체 성공 시 한 번에 커밋, 하나라도 실패하면 전체 롤백
```

질문식 기록만 저장되고 거래가 누락되거나, 거래만 체결되고 잔액이 갱신되지 않는 부분 성공은 허용하지 않는다.

### 6.5 평균 매수가와 손익 계산

#### 매수

```text
새 평균 매수가
= ((기존 수량 × 기존 평균 매수가) + (매수 수량 × 체결가))
  ÷ (기존 수량 + 매수 수량)
```

#### 매도

```text
실현손익 = (매도 체결가 - 평균 매수가) × 매도 수량
```

- 일부 매도 시 남은 수량의 평균 매수가는 유지한다.
- 전량 매도 시 포지션 수량은 0으로 만들고 화면 목록에서는 제외한다.
- 수수료와 세금은 화면에서 실제 수준을 안내하되, MVP 계산 적용 여부는 운영 정책 확정 전까지 별도 필드로 분리한다.

---

## 7. 서비스 내부 API 초안

외부 키움 API 계약과 앱의 사용자 데이터 API를 분리한다.

### 7.1 시세·차트

| Method | 경로 | 설명 |
|---|---|---|
| `GET` | `/api/market/stocks` | 30종목과 최신 현재가 목록 |
| `GET` | `/api/market/stocks/{code}` | 종목 상세 현재가 |
| `GET` | `/api/market/stocks/{code}/chart?period=minute|daily|weekly` | 차트 데이터 |
| `GET` | `/api/market/events` | 실시간 시세 SSE |

### 7.2 모의투자

| Method | 경로 | 설명 |
|---|---|---|
| `GET` | `/api/me/virtual-account` | 가상 현금·총자산 요약 |
| `GET` | `/api/me/positions` | 보유종목 목록 |
| `GET` | `/api/me/trades` | 거래내역과 질문 기록 |
| `POST` | `/api/orders/preview` | 서버 현재가 기준 예상 금액 검증 |
| `POST` | `/api/orders` | 질문 기록을 포함한 가상 주문·체결 |
| `POST` | `/api/behavior-events` | 허용된 행동 이벤트 묶음 저장 |

### 7.3 주문 요청 예시

```json
{
  "idempotencyKey": "client-generated-unique-key",
  "stockCode": "005930",
  "side": "buy",
  "quantity": 2,
  "reason": {
    "type": "knowledge",
    "confidence": 75,
    "note": "집에서 사용하는 제품을 만드는 회사라서 궁금했어"
  }
}
```

가격, 총액, 잔액, 사용자 ID는 클라이언트 입력값을 신뢰하지 않고 서버에서 결정한다.

---

## 8. 행동 이벤트 수집

F2는 다음 이벤트를 자체 DB에 저장해 F9 스코어링과 F10 선제 도움 신호에 활용할 수 있게 한다.

| 이벤트 | 주요 값 | 목적 |
|---|---|---|
| `stock_list_viewed` | 세션, 조회 시각 | 탐색 시작 확인 |
| `stock_detail_viewed` | 종목코드, 진입·이탈 시각, 체류시간 | 종목 탐색 깊이 측정 |
| `chart_period_changed` | 종목코드, 이전·새 기간 | 차트 탐색 기록 |
| `order_side_changed` | 매수·매도, 변경 횟수 | 사용법 헤맴 신호 |
| `order_quantity_changed` | 변경 완료 값, 변경 시각 | 수량 수정 신호 |
| `order_preview_opened` | 종목코드, 수량 | 확인창 반복 신호 |
| `order_completed` | 거래 ID | 주문 완료 연결 |

- 키 입력 한 번마다 전송하지 않고 의미 있는 변경 단위로 묶어 저장한다.
- 이벤트의 목적과 수집 항목을 온보딩 또는 개인정보 안내에서 고지한다.
- 챗봇 대화는 투자성향 스코어링에 포함하지 않는다.
- 행동 데이터는 투자 실력의 점수·등급·채점에 사용하지 않는다.

---

## 9. 오류와 예외 처리

| 상황 | 서버 처리 | 사용자 안내 |
|---|---|---|
| 키움 인증 실패 | 재발급 시도 후 폴백 캐시 확인 | `시세 연결을 다시 확인하고 있어요` |
| 실시간 연결 종료 | 자동 재접속·종목 재등록 | 마지막 갱신 시각 표시 |
| 등록되지 않은 종목 | 주문·조회 거절 | `모의투자에서 제공하지 않는 종목이에요` |
| 현재가 없음 | 주문 중단 | `현재 가격을 확인한 뒤 다시 시도해 줘` |
| 시세가 오래됨 | 운영 정책에 따라 폴백 또는 주문 중단 | 기준 가격과 시각 명시 |
| 현금 부족 | 트랜잭션 롤백 | 부족 금액과 주문 가능 수량 안내 |
| 보유 수량 부족 | 트랜잭션 롤백 | 현재 보유 수량 안내 |
| 중복 주문 | 최초 결과 반환 | 새 거래를 만들지 않음 |
| DB 저장 실패 | 전체 롤백 | `주문이 완료되지 않았어요. 다시 확인해 줘` |
| 질문 필수값 누락 | 주문 전에 검증 실패 | 누락된 선택 항목 강조 |

---

## 10. 보안·개인정보·안전

- 키움 앱키와 시크릿키, DB 관리자 키는 서버에서만 사용한다.
- API는 로그인된 사용자의 가상계좌만 조회·변경할 수 있게 권한을 검사한다.
- 주문의 `user_id`, 체결가, 잔액은 요청 본문 값을 신뢰하지 않는다.
- 한 줄 생각과 이벤트 메타데이터는 허용 필드와 최대 길이를 제한한다.
- 비밀번호를 자체 저장한다면 단방향 해시를 사용한다.
- 미성년 사용자 정보는 최소한으로 수집하고 법정대리인 동의 요건을 별도로 검토한다.
- 모든 거래 화면에 `실제 돈이 아닌 모의투자입니다`를 표시한다.
- AI 또는 안내 문구는 종목 추천·매매 시점·목표가·수익률 전망을 제공하지 않는다.
- 거래 및 질문 기록의 보관 기간과 삭제 정책은 개인정보 정책 확정 시 함께 결정한다.

---

## 11. 화면 및 카피 원칙

- 모바일 390px, 최대 430px 기준으로 설계한다.
- 상승은 빨강 `#E8322E` + `▲`, 하락은 파랑 `#1668DC` + `▼`를 함께 사용한다.
- 현재가와 손익 숫자는 자릿수가 흔들리지 않게 표시한다.
- 한 화면의 주 행동은 하나만 강조한다.
- 주문 전후에 `실제 돈이 아닌 모의투자`임을 명확하게 알린다.
- 질문식 기록은 시험이나 평가처럼 보이지 않도록 `정답은 없어!`를 표시한다.
- 체결 성공에만 가벼운 컨페티를 사용한다.
- `사야 해`, `팔아야 해`, `매수 기회`, `목표가`, `오를 것` 등 방향성 문구를 사용하지 않는다.

---

## 12. 핵심 지표

- 종목 목록 진입 대비 상세 조회율
- 상세 조회 대비 주문 시작률
- 주문 시작 대비 질문식 기록 완료율
- 질문식 기록 완료 대비 체결 성공률
- 첫 매수 완료까지 걸린 시간
- 사용자당 조회 종목 수
- 2개 이상 종목 보유 사용자 비율
- 주문 실패율과 실패 사유 분포
- 중복 주문 차단 건수
- 시세 API 오류율, SSE 재연결 횟수, 마지막 시세 지연 시간
- 거래와 질문 기록의 DB 저장 정합성 오류 건수

수익률 자체는 교육 성과나 사용자 실력의 유일한 지표로 사용하지 않는다.

---

## 13. 골든 패스

1. 민지 계정으로 모의투자에 진입한다.
2. 30종목 목록에서 삼성전자를 검색한다.
3. 삼성전자 상세에서 현재가와 분봉·일봉·주봉 차트를 확인한다.
4. 매수를 선택하고 수량 2주를 입력한다.
5. 서버 최신 시세 기준 예상 금액과 주문 후 현금을 확인한다.
6. 매수 근거 `이 회사나 제품을 잘 알아`, 확신도 `75`, 한 줄 생각을 입력한다.
7. 매수 요청을 한 번 전송하고 시장가로 가상 체결한다.
8. 완료 화면에서 체결 수량·가격·총액·잔액을 확인한다.
9. 포트폴리오에서 보유 수량과 평균 매수가가 반영된 것을 확인한다.
10. 거래내역에서 체결 내용과 질문식 기록을 확인한다.
11. 같은 요청 키를 다시 보내도 거래가 중복 생성되지 않는 것을 확인한다.
12. 일부 수량을 매도하고 현금, 보유 수량, 실현손익이 함께 갱신되는 것을 확인한다.

---

## 14. MVP 완료 기준

1. `kiwoom-stocks.mjs`의 30종목이 목록에 빠짐없이 표시된다.
2. 각 종목의 현재가·등락·갱신 시각이 키움 연동값으로 표시된다.
3. 종목 상세에서 분봉·일봉·주봉 선차트를 전환할 수 있다.
4. 매수·매도 수량과 예상 금액을 확인할 수 있다.
5. 매수 이유·확신도와 매도 이유의 필수 검증이 동작한다.
6. 체결 시 주문·거래·포지션·현금 원장·질문 기록이 하나의 DB 트랜잭션으로 저장된다.
7. 현금 부족, 보유 수량 부족, 미등록 종목, 시세 없음 주문이 차단된다.
8. 중복 요청이 중복 체결을 만들지 않는다.
9. 포트폴리오와 거래내역이 새로고침·재로그인 후에도 자체 DB 값으로 복원된다.
10. 키움 장애 시 마지막 정상 시세 또는 명시된 오류 상태를 제공한다.
11. API 비밀키와 DB 관리자 권한이 클라이언트 번들에 포함되지 않는다.
12. 골든 패스를 재현하고 `web`에서 `npm run build`가 통과한다.

---

## 15. 구현 단계

### 1단계 — 데이터 계약

- 30종목 카탈로그를 앱에서 재사용 가능한 서버 모듈로 연결
- 기존 현재가·차트·SSE 응답 타입 고정
- 자체 DB 제품 확정 및 마이그레이션 작성
- 검증: 30종목 수, 종목코드 6자리, 시세·차트 응답 스키마 테스트

### 2단계 — 가상계좌와 원장

- 사용자별 초기 가상자금 생성
- 주문·체결·포지션·현금 원장 테이블 구현
- 원자적 체결 트랜잭션과 중복 방지 구현
- 검증: 매수·일부 매도·전량 매도·잔액 부족·중복 요청 테스트

### 3단계 — 질문식 기록과 행동 이벤트

- 매수·매도 이유 폼 연결
- 거래와 질문 기록 1:1 저장
- 탐색·주문 UI 이벤트 묶음 저장
- 검증: 필수값, 확신도 허용값, 새로고침 후 기록 복원 테스트

### 4단계 — F2 화면 연결

- 종목 탐색, 상세, 차트, 주문, 완료, 포트폴리오, 거래내역 구현
- 실시간 SSE와 폴백 상태 표시
- 디자인 시스템과 어린이용 카피 적용
- 검증: 모바일 390px에서 골든 패스 재현

### 5단계 — 통합 검증

- 키움 정상·장외·연결 실패 시나리오 확인
- 동시 주문과 중복 클릭 정합성 확인
- DB 레코드와 화면 잔액·보유 현황 대조
- `npm run build` 통과

---

## 16. 구현 전 확정할 항목

1. 자체 DB 제품, 호스팅 위치, 백업·복구 방식
2. 사용자 로그인 및 가족 계정 연결 방식
3. 사용자별 초기 가상자금
4. 수수료·세금을 실제 계산에 반영할지, 안내만 할지
5. 장외 시간의 가상 체결 허용 여부
6. 시세를 오래된 것으로 판단하는 정확한 시간 기준
7. 동일 종목 매도 후 30분 재매수 제한의 실제 적용 여부
8. 포트폴리오 스냅샷 생성 주기
9. 행동 이벤트 및 한 줄 생각의 보관·삭제 기간
10. 외부 공개 시 키움·코스콤 시세 이용 허가 범위

