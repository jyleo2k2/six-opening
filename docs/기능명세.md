# 영웅 키움 — 기능 지도 (현재 구현) v22

> 갱신: 2026-08-16 · 기준: `web/` 실제 코드 (PR #273까지 병합된 `main`) + 라이브 Supabase `profiles` 실조회
>
> 현재 동작을 판단할 때 **프론트 구현 → 백엔드 계약 → 문서** 순으로 확인한다. 제품 목표·법무·레드라인은 `영웅키움_기획_통합문서_v2.md`, 기능 상세는 각 기능의 `SPEC.md`를 따른다.

- 공식 실행 앱: `web/`
- 기능 ID: F0, F2, F3, F9, F10, F11
- 삭제된 ID: F1·F4·F5·F6·F7·F8. 삭제 ID는 재사용하지 않는다.
- `behavior-demo/`는 별도 실험 코드이며 현재 사용자 화면의 정본이 아니다.
- 영웅문·실계좌·실주문·로보마켓은 연결하지 않은 모의투자 데모다.

## 1. 문서 라우팅

| 작업 | 먼저 읽을 단일 명세 |
|---|---|
| F0 홈·런타임 조립 | `web/features/f0-home/AGENTS.md` + 이 문서 |
| F2 탐색·상세·차트·주문 | `web/features/f2-trade/SPEC.md` (화면 본문은 `f0-home`으로 옮겨 왔다 — §3) |
| F3 매매 이유·계획 기록 | `web/features/f2-trade/SPEC.md` |
| F9 성장 아카이브 | `web/features/f9-archive/SPEC.md` (화면 본문은 `f0-home/ArchiveScreen.tsx` — §3) |
| F10 키웅이 | `web/features/f10-chatbot/SPEC.md` |
| F11 가족 기록 | `web/features/f11-feed/SPEC.md` |
| 공용 UI | `docs/디자인시스템.md` + `web/shared/AGENTS.md` |
| 인프라·API·LLM | `docs/기술스택.md` + 해당 기능 `SPEC.md` |

## 2. 실제 런타임 구조

```text
Next.js [[...screen]]
└─ web/app/[[...screen]]/page.tsx   (routeFromPath로 주소 해석 + kw_uid 세션 쿠키 확인)
   ├─ 모르는 주소 → notFound()
   ├─ 비로그인   → LoginGate  (스플래시 + 로그인 폼, SignupWizard 화면)
   └─ 로그인     → ConnectedPrototype  (주소와 무관하게 항상 이것 하나)
      ├─ iframe: /ui/app.html?runtime=1   (살아 있지만 사용자에게 보이지 않는다)
      ├─ 옮긴 화면 오버레이 (iframe 위, PhoneFrame + BottomNav)
      │  ├─ 홈 HomeScreen · 탐색 ExploreScreen · 계좌 PortfolioScreen · 랭킹 RankingScreen
      │  ├─ 아카이브 ArchiveScreen (성향·수익률 2탭 + 카드 모아보기·가족 비교)
      │  ├─ 종목 DetailScreen → ChartScreen(iframe /tradingview-chart) · NewsScreen
      │  └─ 주문 OrderScreen (매수·매도 3단계)
      └─ F10 ChatbotOverlay
```

**화면 해체가 끝났다** [사실 — PR #254]. `ConnectedPrototype`의 `MIGRATED_SCREENS`가 옮긴 목록의 원본이고, 현재 **홈·탐색·종목·계좌·랭킹·아카이브·주문 7개**가 들어 있다(PR #224·#229·#232·#233·#238·#254). `ScreenRoute`가 만들 수 있는 화면이 이 일곱뿐이므로 **어떤 주소로 들어와도 사용자가 보는 것은 React 화면이다.**

F11 가족 기록에는 전용 React 오버레이가 없다. 피드는 `ArchiveScreen`의 **수익률(`return`) 탭** 안에 있다.

**iframe은 아직 살아 있지만 사용자 화면을 그리지 않는다** [사실 — PR #222·#254]. 처음에는 캐치올이 옮긴 화면과 `ConnectedPrototype` 중 **하나만** 렌더했는데, 그러면 화면을 오갈 때마다 iframe이 언마운트돼 `app.html`이 처음부터 다시 떴다. 그 사이 `/api/account` 응답이 오기 전이라 `renderVals-compute.js`가 아이 계정 데모로 폴백해 **남의 계좌가 잠깐 보였다.** 그래서 문서를 살려 둔 채 그 위에 얹는 방식으로 갔고, 주문까지 옮긴 지금은 오버레이가 iframe을 **항상** 덮는다.

남아 있는 이유는 두 가지뿐이다.

- **화면 사각형의 실측 기준이다.** 배율은 `app.html`이 자기 뷰포트로 정하므로 `ConnectedPrototype`이 iframe 안 `#kw-screen`을 `ResizeObserver`로 재고, 그 값으로 챗봇 시트와 폰 프레임 위치를 맞춘다. `display:none`으로 감출 수 없는 것도 이 때문이다.
- **아직 걷어내지 않은 코드다.** `openRoute`의 `setOverlay(null)` 가지, `applyFirstRoute`의 `!isMigrated` 분기, `kiwoom:screen`·`kiwoom:open-route` 수신부는 옮기지 않은 화면이 있을 때의 길이라 지금은 도달하지 않는다. `ui-src/screens`에 남은 `buy.html`·`sell.html`도 오버레이 밑에서 계속 렌더된다.

옮긴 화면의 공용 조각은 `PhoneFrame`(폰 프레임)과 `BottomNav`(하단 탭 4개)다. `BottomNav`는 이동을 직접 하지 않고 `onLeave`로 올린다 — 화면 이동은 `ConnectedPrototype`이 소유하며 문서를 갈아끼우지 않고 주소만 바꾼다. `ui-src`의 매수·매도 사본도 같은 하단 탭 마크업을 자기 안에 갖고 있지만(`renderVals-return-7-shared.js`) 이제 아무도 보지 않는다.

**조립이 깨진 참조를 거부한다** [사실 — PR #244]. PR #238이 아카이브를 옮기며 `buildArchive.js`를 지웠는데 호출부가 `renderVals-compute.js`의 **화면 분기 밖 공통 경로**에 남아, `renderVals()`가 매 렌더에서 던지며 매수·매도 화면이 통째로 죽었다. `npm test`도 `next build`도 `app.html`을 실행하지 않아 아무도 못 잡았다. 이제 `ui-build.mjs`의 build·verify가 **`this.foo()`를 부르는데 `foo`가 어디에도 없으면 조립을 거부한다**.

### 2.1 사용자 라우트

주소는 `features/f0-home/screen-route.ts`가 소유한다. `routeFromPath`가 아는 주소만 앱이고, **나머지는 404다**(오타가 조용히 홈으로 넘어가지 않는다).

| 경로 | 현재 역할 | 렌더링 |
|---|---|---|
| `/` | 홈 | **React `HomeScreen`** (iframe 위 오버레이) |
| `/explore` · `/explore/{섹터}` | 51종 종목 탐색. 섹터 칸은 `rank`(기본)·`watch`·유니버스 섹터 id | **React `ExploreScreen`** |
| `/ranking` | 가족 리그 순위 | **React `RankingScreen`** |
| `/portfolio` | 보유 자산 | **React `PortfolioScreen`** |
| `/archive` · `/archive/{자리}` | 성향·수익률 2탭. 자리는 `report`(기본, 주소에 안 적음)·`return`·`cards`·`family` | **React `ArchiveScreen`** |
| `/stock/{6자리코드}` | 종목 상세 (차트·뉴스 하위 화면 포함) | **React `DetailScreen`** |
| `/buy/{코드}` · `/sell/{코드}` | 3단계 주문·이유 기록 | **React `OrderScreen`** |
| `/tradingview-chart` | `lightweight-charts` 차트. `ChartScreen`이 iframe으로 연다 | React |

- 로그인 화면은 라우트가 아니라 모든 주소의 분기다. 비로그인 상태로 어느 주소에 들어와도 `LoginGate`가 뜨고, 로그인하면 그 주소의 화면으로 간다.
- `/stocks`·`/trade/[symbol]`·`/feed` 라우트는 **없다**. `web/app/` 아래에 있던 `archive/`·`stocks/`·`trade/[symbol]/` 빈 스캐폴드 폴더는 PR #223에서 삭제했다. `/archive`는 캐치올이 받는다.
- 화면끼리의 이동은 `onLeave(경로)` → `ConnectedPrototype.openRoute` 하나로 모이고 `history.replaceState`로 주소만 바꾼다(히스토리를 쌓지 않는다 — 앱 안 뒤로가기와 어긋나므로).
- 화면 ↔ iframe `postMessage` 계약(`kiwoom:screen`·`kiwoom:open-route`·`actionFromRoute`의 `open_screen`)은 **코드에 남아 있지만 이제 사용자 경로에서 발화하지 않는다.** iframe 화면을 아무도 보지 않기 때문이다. 아직 쓰는 것은 `ChartScreen`이 `/tradingview-chart` iframe에 보내는 `kiwoom:chart-options` 한 갈래다.

### 2.2 `app.html` 화면 상태

`app.html`은 여전히 `buy`/`sell` 상태를 그리지만 **오버레이가 항상 그 위를 덮으므로 사용자에게 보이지 않는다**(§2). `home`·`explore`·`detail`·`chart`·`news`·`portfolio`·`archive`·`ranking` 상태와 그 `ui-src/screens/*.html`은 React 이관으로 이미 사라졌고, `ui-src/screens`에 남은 파일은 `buy.html`·`sell.html` 둘뿐이다.

즉 `ui-src` 전체가 **도달 불가 사본**이 됐다. 다만 `app.html`은 아직 로드되고 렌더되므로 조립이 깨지면 화면 사각형 실측이 무너진다 — `ui-build.mjs`의 깨진 참조 거부(아래)는 여전히 필요하다. iframe을 실제로 걷어내는 작업은 화면 사각형 계산을 React로 옮기는 것과 한 묶음이다(§10-6).

하단 탭은 양쪽 모두 **홈 / 모의투자 / 아카이브 / 랭킹** 4개다.

## 3. 기능별 실제 소유권

화면을 React로 옮기면서 **UI 소유가 전부 `web/features/f0-home/`으로 모였다.** 기능 폴더(`f2-trade`·`f9-archive`·`f11-feed`)에는 SPEC·계약·가드가 남고 화면 본문은 옮겨 온 컴포넌트가 갖는다.

| 기능 | 현재 사용자 표면 | 실제 코드 소유 |
|---|---|---|
| F0 홈 | 홈, 내 자산, 리그 진입, 랭킹 | React 호스트 `ConnectedPrototype.tsx`; 화면은 `HomeScreen.tsx`·`lib/home-view.ts`, `PortfolioScreen.tsx`·`lib/portfolio-view.ts`, `RankingScreen.tsx`·`lib/ranking-data.ts`; 폰 프레임은 `PhoneFrame.tsx`·`lib/phone-frame.ts` |
| F2 모의투자 | 탐색, 상세, 뉴스, 차트, 주문, 포트폴리오 | UI는 전부 `f0-home` — `ExploreScreen`·`DetailScreen`·`ChartScreen`·`NewsScreen`·`OrderScreen`(+ `lib/explore-cards.ts`·`lib/stock-news.ts`·`lib/stock-chrome.tsx`·`lib/order-view.ts`·`lib/pending-orders.ts`); 차트·시세·뉴스 계약과 예약 규칙은 `web/features/f2-trade/` |
| F3 이유 기록 | 매수 이유·계획, 매도 이유·계획 변경 | UI와 저장은 `OrderScreen`·`lib/order-view.ts`; 문구는 `shared/data/trade-copy.js`; `web/features/f3-reason/`에는 현재 가드만 존재 |
| F9 아카이브 | 성향·수익률 2탭과 오버레이 2종 | UI는 `f0-home/ArchiveScreen.tsx`, 값은 `lib/archive-profile-view.ts`·`lib/archive-feed.ts`, 조회는 `lib/use-archive-data.ts`; 계산 원본은 `web/shared/engine/`, API는 `/api/profile*`·`/api/family` |
| F10 키웅이 | 전역 React 오버레이 | `web/features/f10-chatbot/` + `web/shared/llm/` + `/api/chat`. **답변 밑 화면 이동 버튼은 걷어냈다**(PR #260) — 서버가 만드는 `uiAction` 데이터·타입·허용 화면 판정은 되돌리기 쉽게 남기고 말풍선 렌더링에서만 뺐다 |
| F11 가족 기록 | 아카이브 수익률 탭 | UI는 `ArchiveScreen`의 수익률 탭 + `lib/archive-feed.ts`; `web/features/f11-feed/`에는 SPEC·가드만 있다. `/api/family`·`/api/comments`·`/api/likes` |
| 로그인·가입 | `/` 진입 시 로그인 화면 | `web/app/LoginGate.tsx`·`SignupWizard.tsx` + `/api/auth/login`. 로그아웃은 `HomeScreen`의 메뉴가 부른다 |

## 4. 현재 데모 전제

| 항목 | 코드 기준 현재값 |
|---|---|
| 로그인 | 아이디·비밀번호로 `POST /api/auth/login`. 세션 쿠키가 없으면 앱에 들어갈 수 없다(자동 로그인 폴백 제거). **앱을 다시 열면 다시 로그인한다**(§4.3) |
| 가입 | 부모 4단계·자녀 3단계 마법사 **화면만 만드는 것이 확정된 범위다**. 서버 저장 호출은 넣지 않는다(§4.1) |
| 계정별 홈 | 로그인 계정에 따라 아빠·엄마·아이 홈을 각각 다르게 그린다 |
| 메인 자녀 | `김찬영` |
| 가족 | 아빠·엄마·찬영 셋 모두 DB 계정이 있다. `profiles` = 김찬영(`cksdud`/child)·찬영엄마(`mom`/parent·`guardian_role=mom`)·찬영아빠(`dad`/parent·`guardian_role=dad`), 모두 `family_tag='찬영가족'`. 빈 DB에서도 마이그레이션이 세 계정과 보유를 심는다(`seed_family_profiles`·`seed_family_portfolios`) |
| 가상 지갑 | 구성원당 **10,000,000원**. 시드·총자산 산식은 `web/shared/store/prototype-account.js` 한 곳이 소유한다(`app.html`과 옮겨 온 React 화면이 같이 쓴다) |
| 투자 유니버스 | 51종, 13개 섹터 |
| 시세 | `/api/universe` 초기 조회 + `/api/universe/data` 5초 갱신 |
| 주문 | 정규장 시장가는 즉시 체결, 장외 시장가는 다음 거래일 시가 예약, 지정가는 pending 예약. **예약은 `POST /api/orders` 접수가 곧 주문이다**(§6.4) |
| 수수료·세금 | 계산하지 않음. 쓰이지 않던 `FEE` 상수는 PR #226에서 삭제했다 |
| 거래 단위 | 금액 또는 주 수. 소수 수량이 생길 수 있음 |
| 스쿨락 | **꺼져 있다**(§4.4). `lib/use-wallet.ts`의 `SCHOOL_LOCK_ENABLED = false` 하나가 정하고, `isSchoolTime`은 항상 거짓이라 자녀도 시간과 무관하게 주문할 수 있다 |
| 계정 전환 | **로그아웃 후 다시 로그인이 확정된 방식이다**(§4.2). 전용 스위처 UI는 만들지 않는다. F11의 구성원 필터는 열람 기준만 바꾼다 |
| 폼팩터 | 바깥 프레임 450×920, 내부 앱 402×874 |

첫 진입 튜토리얼은 현재 사용자 흐름에 없다. 어디서도 로드하지 않던 `web/public/ui/tutorial.js`는 삭제했다.

### 4.1 가입 마법사는 목업이다 [사실]

부모 4단계·자녀 3단계 가입 마법사(`web/app/SignupWizard.tsx`)는 **화면 시연용 목업으로 확정됐다.** 서버 저장을 붙이지 않는 것이 결정이며, 미구현 결함이 아니다.

- 앱 안에서 새 가족·새 계정을 만드는 흐름은 없다. 계정은 Supabase에서 직접 만든다.
- 마법사 완료는 골든 패스에 넣지 않는다.
- `POST /api/auth/signup` 같은 경로를 새로 만들지 않는다.

### 4.2 계정 전환은 로그인·로그아웃이다 [사실]

다른 가족 구성원 화면을 보는 방법은 **로그아웃 → 해당 계정으로 재로그인** 하나로 확정됐다. 앱 안 계정 스위처 UI는 만들지 않는다.

- 로그아웃은 홈 화면(React `HomeScreen`) 메뉴의 `DELETE /api/auth/login`이다.
- `POST /api/auth/switch`는 **PR #221에서 삭제했다.** 어느 화면도 부르지 않았고 이 방식이 확정된 이상 살릴 계획이 없었다. `web/app/api/auth/`에는 `login`만 남는다.
- F11 수익률 탭의 구성원 필터는 열람 대상만 거르며 세션(`kw_uid`)을 바꾸지 않는다. 이는 전환이 아니다.

### 4.3 세션은 서버 부팅 표식이 정한다 [사실 — PR #240·#245]

앱을 다시 열면 로그인 화면부터 시작한다. PR #240이 `kw_uid`에서 30일 `Max-Age`를 빼 브라우저 세션 쿠키로 바꿨지만 그것만으로는 부족했다 — `Set-Cookie`는 새로 발급하는 쿠키에만 적용돼 **이미 나간 30일짜리 쿠키가 그대로 남았고**, 크롬·엣지의 "이전에 열었던 페이지 계속 보기"는 세션 쿠키까지 복원했다.

그래서 언제 로그아웃할지를 브라우저가 아니라 **서버가 정한다.**

- 프로세스가 뜰 때 부팅 표식을 만들어 쿠키 값에 `사용자id.표식`으로 넣고, 읽을 때 지금 표식과 대조한다. 다르거나 없으면 비로그인이다(표식 없는 옛 쿠키도 함께 걸러진다).
- 표식은 `globalThis`에 둔다. Next가 서버 컴포넌트와 라우트 핸들러를 다른 모듈 그래프로 불러도 기준이 갈리지 않고, 개발 중 모듈이 다시 평가돼도 작업하다 말고 로그아웃되지 않는다.
- 쿠키 해석은 `app/api/supabase.ts`의 `sessionUserIdFromCookie` 하나가 한다. 화면(`page.tsx`)과 API가 같은 함수를 쓴다.
- 서버를 재시작하면 모든 세션이 끊긴다. 개발 중 `npm run dev`가 다시 뜰 때마다 다시 로그인해야 한다.

### 4.4 스쿨락은 꺼 두기로 했다 [사실 — 2026-08-16 확정]

자녀 매매 잠금(평일 09:00~15:30)은 **끈 상태가 현재 결정이다.** `lib/use-wallet.ts`의 `SCHOOL_LOCK_ENABLED = false` 한 곳이 정한다.

끈 이유는 **그 창이 정규장 창(`isRegularMarketOpen`)과 정확히 같기 때문**이다.

| 시간대 | 스쿨락이 켜져 있었을 때 자녀가 할 수 있는 것 |
|---|---|
| 평일 09:00~15:30 | 주문 단계(`locked && step === 2`)에서 막혀 **질문식 매매를 끝까지 진행할 수 없다** |
| 그 밖 | 주문은 되지만 정규장이 아니라 다음 거래일 시가 예약이 된다 |

즉 **자녀 계정으로 즉시 체결을 볼 수 있는 시간대가 존재하지 않았다.** 두 창이 같다는 사실은 `use-wallet.test.ts`가 지킨다 — 어느 한쪽 창이 바뀌면 그 테스트가 깨지고, 그때 다시 켤지 검토한다.

보여 줄 기능도 아니다. 중간발표 대본 v2 12슬라이드에 등장하지 않고, 통합문서 v2에는 "스쿨락 15:00 / 문구 16:00 불일치 해소" 체크리스트로 한 번 나오며, 골든 패스(§9)에도 단계가 없다. 와이어프레임브리프는 스쿨락 카드가 부모 전용이고 일반 사용자 흐름에서 열 수 없다고 적는다.

- 화면의 `locked` 분기는 지우지 않았다. 종목 상세 하단(`lib/stock-chrome.tsx`)과 계좌(`PortfolioScreen`)의 `지금은 학교에서 공부할 시간!` 안내는 코드에 남아 있고 지금은 그려지지 않는다.
- 되살리려면 `SCHOOL_LOCK_ENABLED`만 `true`로 되돌린다.
- **챗봇은 아직 이 기능을 안내한다** — §10-4.

## 5. 주문·기록 흐름

### 5.1 매수

1. 시장가 또는 지정가와 금액/수량을 고른다.
2. 6개 매수 이유 중 하나를 고르고 메모를 선택 입력한다.
3. 4개 보유 계획 중 하나를 고른 뒤 주문한다.
4. 정규장 시장가는 즉시 체결한다. 장외 시장가는 현금을 예약한 `scheduled` 주문을, 지정가는 현금을 예약한 `pending` 주문을 **서버에 접수한다**(§6.4).

매수 이유 버튼 순서는 진입할 때마다 섞는다(F3 SPEC). 매수 완료에는 풍선·색종이·곰 등장 연출이 붙고, 매도 완료에는 원본대로 연출이 없다 [사실 — PR #256에서 `phone-frame.css`에 keyframes를 복원했다. 이관 과정에서 마크업만 남고 정의가 사라져 조용히 죽어 있었다].

현재 매수 이유는 뉴스·차트·익숙함·랭킹·주변 의견·직감 6개다. 계획은 단기·시즌·목표가·미정 4개다. **확신도 입력은 없다.** 목표가는 사용자가 스스로 적는 보유 계획이며 AI 추천 목표가가 아니다.

### 5.2 매도

1. 시장가 또는 지정가와 금액/수량을 고른다.
2. 6개 매도 이유 중 하나를 고른다.
3. 기존 계획 준수 여부를 기록하고, 계획과 다르면 5개 변경 이유 중 하나를 고른다.
4. 정규장 시장가는 즉시 반영한다. 장외 시장가는 수량을 예약한 `scheduled` 주문을, 지정가는 `pending` 주문을 **서버에 접수한다**(§6.4). 매도 이유 버튼은 앞 5개만 섞는다.

장외 시장가 예약은 주문 접수와 실제 체결을 분리한다. 휴일·거래정지·시가 데이터 미확인 때는 예약을 유지하고, 같은 주문을 중복 체결하지 않는다. 금액 매수는 예약 금액 전부를 시가 기준 소수 수량으로 바꾸며, 수량 매수는 시가 상승으로 예약 금액을 넘으면 거절하고 현금을 돌려준다. 매도 예약과 지정가 매도는 보유 수량에서 사용 가능 수량만 잠그며 취소 시 자산 총액이 변하지 않는다. 지정가 주문은 자동 체결되지 않는다.

## 6. 저장과 서버 동기화

### 6.1 브라우저 즉시 상태

브라우저에 남는 것은 **지갑 하나뿐**이다.

| 저장소 | 키 | 값 | 수명 |
|---|---|---|---|
| `localStorage` | `kw_proto_v1` | 계정별 현금·보유, 매수·매도 기록, 행동 이벤트, 시퀀스, 관심 종목 | 새로고침해도 유지 |

같은 칸에 미체결 주문(`pending`)도 딸려 저장되지만 **원본이 아니라 캐시다.** `GET /api/orders`를 못 읽었을 때만 그 값을 남긴다 — 조회 한 번 실패했다고 예약이 사라진 것처럼 보이면 안 되기 때문이다(`lib/server-account.ts`).

**화면 임시값을 넘겨받던 `sessionStorage` 두 칸(`kw_proto_ui_v1`·`kw_proto_nav_v1`)은 사라졌다.** 예전에는 옮긴 화면으로 갈 때 `top.location.href`로 문서를 새로 받았고, 그러면 `app.html`이 처음부터 다시 떠서 작성 중이던 주문이 날아갔기 때문에 백업이 필요했다(PR #203). 지금은 부모가 iframe을 살려 둔 채 옮긴 화면을 그 위에 얹으므로 **문서가 안 바뀌고 메모리도 안 죽는다** — 백업할 이유가 없다. `screen-state-handoff.test.ts`가 이 계약을 지킨다(두 키가 `app.html`에 다시 나타나면 실패한다).

화면 이동은 이제 React 쪽 `onLeave(path)` 하나다. `ui-src`의 `leaveToRoute(path)`(→ `kiwoom:open-route`)는 코드에 남아 있지만 그 화면을 아무도 보지 않으므로 발화하지 않는다.

**탭 유효 열람(10초) 버퍼도 React가 들고 있다** [사실 — PR #256]. `lib/tab-views.ts`가 상세·차트·뉴스 열람을 모아 두었다가 **매수 체결 때** `/api/tab-view`로 보낸다. 예전에는 iframe 메모리가 버퍼였는데 매수가 React로 옮겨 가며 비울 방아쇠가 사라져 열람 기록이 영영 서버에 닿지 않았다. 로그인 역할이 맞지 않으면 보내지 않고 **버린다** — 남겨 두면 다음 사용자의 체결에 남의 열람이 딸려 간다.

배지 적립은 폐기돼 더 이상 저장하지 않는다. 스쿨락도 저장할 설정이 없다 — 꺼진 채 고정이다(§4.4). **"F5 하면 처음부터"**는 진짜 새로고침에만 걸리고, 그때는 지갑만 되살아나므로 화면은 홈에서 시작한다.

읽는 쪽은 `shared/store/prototype-account.js`의 `restorePrototypeState()`(= `readPersistedWallet`) 하나이고 **첫 렌더 전에** 부른다(PR #217). `componentDidMount`에서 되살리던 예전 방식은 시드 지갑이 한 프레임 먼저 보였다 바뀌었다. 옮겨 온 화면은 같은 칸을 `useWallet`으로 읽고 쓴다.

### 6.2 서버 연결

| 흐름 | 현재 동작 |
|---|---|
| `/api/auth/login` | 로그인(POST)은 `LoginGate`가, 로그아웃(DELETE)은 `HomeScreen` 메뉴가 호출한다 |
| `/api/account` | DB 사용자를 찾고, **로그인한 역할의 현금·보유를 서버 값으로 덮어쓴다.** 사용자 화면은 `useWallet`→`applyServerAccount`로 읽는다(PR #243). 반대쪽 역할은 로컬 데모 값으로 남는다 |
| `/api/trade` | DB 사용자와 현재 역할이 맞는 **시장가 즉시 체결**만 best-effort 전송. 실패해도 로컬 체결을 되돌리지 않음(§10-1) |
| `/api/orders` | **미체결 주문의 원본.** GET은 목록 조회이자 **만기 예약 정산 트리거**(멱등한 `settle_order`를 먼저 돌린다), POST는 지정가·장외 예약 접수(`reserve_order`가 한 트랜잭션에서 자원 잠금), DELETE는 취소(`cancel_order`가 `user_id`까지 대조). `useWallet`이 GET, `OrderScreen`이 POST, `PortfolioScreen`이 DELETE를 부른다 |
| `/api/tab-view` | 종목 상세 탭 열람을 카테고리·종목별 카운트로 적재. 버퍼는 `lib/tab-views.ts`이고 매수 체결이 방아쇠다(§6.1) |
| `/api/profile/behavior` | 로그인 세션의 DB 행동 집계로 캐릭터 카드만 다시 정함 |
| `/api/profile/season-cards` | 로그인 세션의 DB 매수를 주 단위로 묶은 지난 주차 카드 |
| `/api/family` · `/api/comments` · `/api/likes` | 가족 구성원·거래와 코멘트·좋아요. **원본이 DB다** |
| `/api/news`, `/api/news/[newsId]` | 검수를 통과한 어린이 뉴스 목록·상세 |

즉, **미체결 주문·로그인·가족·반응·성향 조회는 DB가 원본**이고, 아직 브라우저가 원본인 것은 **체결된 거래와 그로부터 나오는 현금·보유**뿐이다.

### 6.3 원본이 두 갈래인 이유와 현재 위치 [사실]

`app.html`은 원래 서버 없이 `localStorage` 하나로 도는 프로토타입이었다. 이후 기능별로 Supabase를 붙였지만 **기존 로컬 상태를 걷어내지 않고 위에 얹었기 때문에** 원본이 둘로 갈렸다. 의도한 설계가 아니라 **이행이 절반 끝난 상태**다.

| 데이터 | 현재 원본 | 배경 |
|---|---|---|
| 현금·보유·매수매도 기록·행동 이벤트 | `localStorage` `kw_proto_v1` | 프로토타입 때부터 있던 경로 |
| 로그인·가족·코멘트·좋아요·행동 집계·뉴스 | Supabase | 나중에 DB부터 만든 경로 |
| **미체결 주문(지정가·장외 예약)** | **Supabase** | **PR #246·#250이 로컬에서 걷어내 서버로 옮겼다(§6.4)** |

쓰기 경로는 **즉시 체결에 한해** 로컬 먼저, 서버는 조건부 best-effort다.

- `persist()`가 무조건 먼저 `localStorage`에 쓰고, 화면은 그 값을 읽는다.
- `saveTrade()`는 `dbSyncable()`(= DB 사용자 로드됨 **그리고** 현재 화면 계정 == 로그인 역할)일 때만 `POST /api/trade`를 보내고, 실패는 삼킨다.
- 따라서 조건이 어긋난 체결은 로컬에만 남고 DB에는 영영 들어가지 않는다.

읽기 경로는 한 방향으로 좁혀졌다. **로그인한 역할의 현금·보유는 DB 값으로 덮어쓴다.** 그래서 DB 저장이 실패한 체결은 화면상 체결로 보이다가 **다시 로드하면 사라질 수 있다**(§10-1). 근본 해결은 로컬 상태를 걷어내고 DB를 유일 원본으로 만드는 것이다.

옮겨 온 화면이 저장소만 읽던 동안에는 여기가 더 나빴다 [사실 — PR #243]. `app.html`의 `applyServerHoldings`는 `this.setState`로 **메모리만** 갱신하고 `persist()`를 거치지 않아 `kw_proto_v1`에 서버 값이 남지 않는다. 옮겨 온 화면은 그 메모리를 볼 수 없어 계좌·상세·탐색·아카이브가 DB 보유 대신 `seedAccounts()` 하드코딩 값을 그렸다(직접 `/api/account`를 읽던 홈만 맞았다). 지금은 `useWallet`이 같은 응답을 읽어 **응답을 기다렸다 한 번에** 세운다 — 저장소 값을 먼저 그리고 덮으면 시드 지갑이 한 프레임 보였다 바뀐다.

### 6.4 서버 주문 생애주기 — 화면까지 연결됐다 [사실 — PR #246·#250]

마이그레이션 `20260815033308_order_lifecycle_db_ownership`이 `transactions`에 주문 생애주기를 넣었고, PR #250이 화면을 그 위에 붙였다. **미체결 주문의 원본은 이제 서버 하나다.**

- 상태 다섯 개는 화면이 쓰던 값을 그대로 옮겼다: `filled` · `pending`(지정가 대기) · `scheduled`(다음 거래일 시가) · `cancelled` · `rejected`.
- 예약 자원은 총량에서 잠근 양을 빼는 방식이다 — 현금은 `account.reserved_balance`, 수량은 `holdings.reserved_quantity`. 잔고를 직접 깎지 않으므로 예약·취소가 총자산을 바꾸지 않는다.
- **`transactions`에 미체결 행이 섞이므로 거래를 읽는 모든 경로는 `order_status='filled'`을 걸러야 한다.** 공용 헬퍼는 `app/api/supabase.ts`의 `selectFilledTrades`이며, 주문 자체를 다루는 `api/orders`만 `selectRows`를 직접 쓴다.
- **접수가 곧 주문이다.** `OrderScreen`은 예약 목록을 만들지도, 현금을 빼지도 않는다 — `POST /api/orders`가 실패하면 완료 화면 대신 거절 문구를 보여 주고, 성공하면 `refresh()`가 계좌와 주문 목록을 서버에서 다시 읽는다.
- **조회가 정산을 겸한다.** `GET /api/orders`가 만기 지난 예약을 먼저 처리하므로 `useWallet`의 조회 자체가 체결 트리거다. 화면은 시가를 따로 확인하지 않고, `settle_order`가 멱등이라 동시에 두 번 들어와도 중복 체결되지 않는다.
- 서버 응답을 화면이 쓰던 `pending` 모양으로 바꾸는 곳은 `features/f2-trade/lib/scheduled-orders.js`와 `f0-home/lib/pending-orders.ts`다. **화면이 스스로 정산하지 않는다는 계약을 `scheduled-orders.test.ts`가 지킨다** — 실제로 PR #252가 남긴 주석 한 줄이 이 가드에 걸려 `main`의 `npm test`가 깨진 적이 있다(PR #256에서 해소).

남은 것은 즉시 체결이다. 시장가 체결은 여전히 로컬에 먼저 쓰고 `/api/trade`로 best-effort 전송한다(§6.3·§10-1).

## 7. F9 계산과 표시

아카이브는 React `ArchiveScreen`이며 **자리 넷**이다 — 성향 탭, 수익률 탭, 그리고 탭을 덮는 카드 모아보기·가족 비교(시트가 아니라 자리다). 카드 모아보기는 좌우로 끌어 넘기는 레일 하나이고(PR #239·#241), 예전의 별도 카드 상세 자리는 그 레일에 합쳐졌다.

계산 엔진은 **두 벌이며 역할이 다르다**. 로그인 상태의 정본은 `shared/engine/behavior-profile.ts`(전 축 **0~10**, 5가 중립)이고 화면이 이를 그대로 읽는다. `shared/engine/archive-profile.js`(0~100)는 비로그인이거나 서버 응답이 오기 전의 폴백과 행동 신호 캐릭터 판정에 남아 있다.

| 경로 | 무엇을 주나 |
|---|---|
| `shared/engine/behavior-profile.ts` | 로그인 정본 — 다섯 축 0~10, 캐릭터, 정확 레벨, 주간 결산 카드 |
| `shared/engine/archive-profile.js` | 비로그인·응답 전 폴백 — `kw_proto_v1`로 계산하는 0~100 값 |
| `GET /api/profile/behavior` | 로그인 세션의 행동 집계로 **캐릭터 카드만** 다시 정한다 |
| `GET /api/profile/season-cards` | 로그인 세션의 Supabase 기록을 KST 주 단위로 묶은 지난 주차 카드 |
| `GET /api/family` | 실제 가족 구성원, 누적 성향, **구성원별 수익률**(가족 비교 오버레이 + 수익률 탭 달리기 트랙) |
| `shared/engine/portfolio-return.ts` | 평가액·원금·수익률 산식. 금액과 비율을 나눠 돌려주므로 서버가 타인에게는 `returnRate`만 넘길 수 있다 |

아카이브가 부르는 서버 경로는 `lib/use-archive-data.ts` 하나로 모여 있다 — `GET /api/profile/behavior`·`GET /api/profile/season-cards`·`GET /api/family`, 그리고 피드 반응의 `/api/comments`·`/api/likes`. 주차 카드 산식은 `computeBehaviorProfile` 한 벌이다.

### 7.1 F9는 LLM을 쓰지 않는다 [사실 — 2026-08-15 확정]

아카이브에 보이는 값은 **전부 `shared/engine` 계산 결과**다. 캐릭터 카드에 `gpt-5.6-luna`가 쓴 설명을 붙이는 "Luna 서술" 경로(`POST /api/profile` + `f9-archive/lib/narration.ts`)가 있었지만 **어느 화면도 부른 적이 없어 PR #221에서 삭제했다.** 함께 지운 것은 그 라우트만 쓰던 `behavior-profile.ts`의 `parsePrototypeProfileInput`·`profileEntriesFromTrades`다.

서술을 다시 붙이기로 하면 SPEC에 계약부터 적고 화면 연결까지 한 작업으로 처리한다. 예전 구현을 되살리지 않는다.

- 유효 정보 확인: 매수 전 본 서로 다른 종목 상세 탭 수
- 집중/분산: 유효 섹터 수와 투자 비중
- 정확: 정본은 체결 후 **2거래일**, 폴백은 **5거래일** 종가 비교
- 매수 표본이 3건 미만이면 분석 데이터가 부족한 상태로 본다.

화면은 `LV` 레벨 표기를 쓴다. 폐기된 5축(`AXES`)과 별(★) 표기는 현재 실행 경로에서 사라졌고 레거시 목업 파일에만 남아 있다. 가족 비교의 동의 문구는 실제 동의 데이터가 아닌 고정 문구다. 상세 계산은 F9 SPEC을 따른다.

## 8. F11 피드와 차트 마커

- 피드: `GET /api/family`가 세션 사용자와 같은 `family_tag`의 구성원·Supabase 체결을 최신순으로 준다. 로컬 시드 피드와 `kiwoom-family-feed` 저장소는 제거됐다
- 차트 마커: `/api/trades`가 돌려주는 Supabase 거래만 사용
- 코멘트·좋아요: `/api/comments`·`/api/likes`를 통해 DB에 저장한다. 좋아요는 가족 전체 개수와 본인 선택 상태를 유지하고, 코멘트는 본인 것만 삭제할 수 있다
- 타인 카드의 수량·체결가는 서버 응답 단계에서 `null`로 마스킹한다
- 부모→자녀 코멘트의 추천·타이밍·훈계·채점 표현은 서버가 저장 전에 차단한다
- 피드는 `ArchiveScreen`의 수익률 탭 **한 곳에만** 있다(카드 조립은 `lib/archive-feed.ts`). 예전 React 오버레이 `FeedScreen`은 어디서도 import되지 않아 삭제했다
- 표시는 `tradedAt` 내림차순 **최근 12건**까지이고 종목 이동 버튼은 없다

피드와 차트가 이제 같은 거래 원본(Supabase `transactions`)을 본다. 상세 계약은 F11 SPEC을 따른다.

## 9. 현재 도달 가능한 골든 패스

1. `/`에서 아이디·비밀번호로 로그인하면 계정에 맞는 홈이 열린다.
2. 홈에서 가족 순위와 자녀 자산을 본다.
3. 모의투자 탭에서 종목을 탐색하고 상세 탭을 10초 이상 확인한다.
4. 상세에서 키웅이를 열어 허용 질문을 묻는다.
5. 정규장에는 매수 화면에서 시장가·금액, 이유, 보유 계획을 선택해 즉시 체결한다. 장외에는 같은 주문이 다음 거래일 시가 예약으로 **서버에 접수**되는지 확인한다.
6. 포트폴리오에서 새 거래와 기다리는 주문을 보고, 예약 하나를 취소해 잔액이 돌아오는지 본다. 아카이브에도 반영됐는지 확인한다.
7. 아카이브 수익률 탭에서 가족 구성원의 DB 거래를 보고 좋아요와 코멘트를 남긴다. 다시 열어도 유지된다.
8. 브라우저를 닫았다 다시 열면 로그인 화면부터 시작한다(§4.3).

가입 완료, 튜토리얼, 지정가 자동 체결, 실제 시간외 거래는 이 골든 패스에 넣지 않는다. 계정 전환은 로그아웃 후 재로그인(§4.2)이므로 1번 단계로 되돌아가는 것과 같다.

## 9.1 검증 장치 [사실 — PR #257]

`.github/workflows/web-ci.yml`이 PR마다 `web`에서 **`npm test` → F10 회귀 → `npm run build`**를 돌린다. 그전까지 CI는 세션 정책만 검사해 코드가 빌드되는지조차 보지 않았다.

- `app.html`은 저장소에 없다. `pretest`·`prebuild`가 `ui-src`에서 먼저 조립하므로 **조립이 깨지면 CI가 그 자리에서 실패한다**(§2.2의 깨진 참조 거부와 짝이다).
- 비밀키 없이 통과해야 한다. 빌드가 환경변수를 요구하기 시작하면 CI가 먼저 잡는다.
- F10 회귀는 `records/f10-child-sim/run-regression.ts`이며 세트가 둘이다. `questions-v2.json`은 신규 기능 영역을, `questions-v3.json`(172건)은 **같은 취지·다른 표현** 축을 본다. 두 세트는 서로를 대체하지 않는다.
- **차단은 개별 케이스가 아니라 분포로 막는다.** 트리거를 넓히는 작업의 위험은 과차단이 아니라 과소차단이라, 600건에서 `refusal + safety >= 258`을 깨면 실행기가 exit 1 한다.

`web/design-system/`은 claude.ai/design에서 받아 온 **반입 원본**이다(PR #252). 빌드에 들어가지 않고 손으로 고치지 않으며, 화면이 쓰는 토큰·컴포넌트의 단일 원본은 여전히 `docs/디자인시스템.md`다.

## 10. 문서화된 현재 제약

여기 있는 항목은 **고쳐야 할 것**이다. 목업으로 확정된 가입 마법사(§4.1)와 로그인·로그아웃으로 확정된 계정 전환(§4.2)은 설계 결정이므로 제약이 아니다.

1. **즉시 체결은 로컬 성공과 서버 저장 성공이 원자적으로 묶여 있지 않다**(§6.3). 예약 주문은 서버로 옮겼지만 시장가 체결은 아직 `localStorage`에 먼저 쓰고 `/api/trade`는 조건부 best-effort라, 조건이 어긋나거나 요청이 실패한 체결은 화면에는 남고 DB에는 없다 — 다시 로드하면 `/api/account`가 덮어써 사라진다.
2. F11 카드의 금액(`archive-feed.ts`의 `avgText`)은 총 거래금액이 아니라 **주당 체결가**인데 라벨이 그 구분을 하지 않는다. 매수·매도 양쪽 모두 해당하고, 남의 카드는 서버 마스킹으로 `비공개`라서 본인 카드에서만 보인다.
3. `profiles.login_password`가 아직 평문이다. 세 계정을 심는 마이그레이션은 생겼지만(`20260815033156_seed_family_profiles.sql`) 비밀번호는 커밋할 수 없어 `CHANGE_ME` 자리표시자이고, 실제 값은 배포 후 콘솔에서 직접 넣어야 한다.
4. **챗봇이 없는 기능을 안내한다.** `shared/data/chatbot-knowledge.ts`의 `trade-lock`이 "주문 잠금은 **보호자가 정한 시간** 동안 자녀 계정의 주문만 잠시 막는 기능"이라 답하고 `actionTarget: "home"`으로 홈을 가리킨다(라우팅 테스트에도 `학교 시간엔 매매 쉬기 토글은 어떻게 써?` 케이스가 있다). 그런데 `HomeScreen`에 그런 토글이 없고, 스쿨락은 애초에 보호자가 정하는 값이 아니라 하드코딩된 시계였으며 지금은 아예 꺼져 있다(§4.4). 답변 문구를 실제 동작에 맞추거나 홈에 보호자 설정을 만들거나 둘 중 하나가 필요하다.
5. 아카이브의 `보유 종목 · 섹터별` 레일은 이관하면서 옮기지 않았다. 도달 불가였던 `secModal*`·`retSectors`는 함께 사라졌으므로 되살리려면 레일부터 다시 설계해야 한다.
6. **iframe과 `ui-src` 전체가 도달 불가 사본으로 남아 있다**(§2·§2.2). 걷어내려면 화면 사각형 실측(`#kw-screen` `ResizeObserver`)을 React로 옮기고, 함께 죽은 코드(`MIGRATED_SCREENS` 밖 분기, `kiwoom:screen`·`kiwoom:open-route` 수신부, `ui-src/screens/buy.html`·`sell.html`)를 한 작업으로 지워야 한다. 그때까지는 조립기(`ui-build.mjs`)와 `app.html` 렌더가 계속 유지 비용을 만든다.
7. `web/app/[[...screen]]/page.tsx`의 주석이 아직 "옮긴 화면은 `/ranking` 하나"라고 말한다. 동작에는 영향이 없지만 iframe 철거 작업에서 함께 고친다.

PR #245 이후 해소된 항목: 매수·매도 이관(#254), `/api/orders` 화면 연결(#250), 주문 생애주기 로컬 잔재 제거(#246·#250), 화면 해체 완료(#254).
