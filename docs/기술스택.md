# 기술 스택 (개정 2026-08-13 — 실제 구현과 정합 맞춤)

## 결정

| 항목 | 결정 | 근거 |
|---|---|---|
| 프레임워크 | **Next.js (App Router) + TypeScript** | 단일 레포·단일 실행. 서버 코드로 LLM/시세 키 보호. Vercel 배포 → 발표 때 QR로 실기기 시연 |
| 스타일 | Tailwind CSS — **버전이 앱마다 다르다**. `web`은 v4로 설정 파일 없이 `app/globals.css`의 `@theme`에 토큰을 둔다. `mock-stock`은 v3로 `tailwind.config.ts`에 둔다 | 디자인 토큰을 한곳에 모아 강제(`docs/디자인시스템.md`), 병렬 작업 시 CSS 충돌 없음. **[미해결]** iframe으로 띄우는 `web/public/ui/app.html`은 Tailwind 밖이라 토큰 강제를 받지 않는다 |
| 화면 구성 | **정적 HTML 프로토타입 + React 오버레이**. `web/app/page.tsx` → `features/f0-home/ConnectedPrototype` → `/ui/app.html`을 iframe으로 띄우고 그 위에 F10 챗봇만 React로 올린다. 화면 맥락은 `postMessage("kiwoom:chat-context")`로 전달한다 | 목업 HTML이 화면 본문을 소유한다. `shared/ui`에는 실제 소비자가 있는 `Button`·`Card`·`TabBar`·`ScreenHeader`·`PhoneShell` 5종만 있고, F10 바텀시트는 기능 폴더가 소유하는 React UI다 |
| 상태 | zustand + localStorage persist | 데모 규모에 DB 과잉. 민지↔엄마 스위처·거래 로그·확신도·탐색/주문 이벤트·챗봇 UI 상태를 클라이언트에 저장 |
| 시세 | **키움 REST API** (사용자 확정 — 키움 발표 프로젝트 브랜드 정합) | 국내주식 현재가·일봉. **[확인 필요]** 모의투자 환경 지원 범위·앱키 발급 절차 — 발급 전까지 픽스처로 개발 |
| 시세 폴백 | **필수** — 마지막 성공 응답을 캐시에 저장, API 실패·장외 시간엔 캐시 서빙 | 발표 시각이 장외이거나 행사장 네트워크 불안일 때 데모가 죽으면 안 됨 |
| 차트 | **lightweight-charts v5 확정** (recharts 미채택). 소유는 `web/features/f2-trade/TradingViewChart.tsx`이며 `web/app/tradingview-chart`를 통해 화면 정본의 차트 영역에 연결한다. 분·일·주 기간과 선·캔들 유형 전환을 지원하고 시세는 `web/app/api/quote/[symbol]/chart`만 사용한다 | 종목 상세용 TradingView 경량 차트. 기존 인라인 SVG 캔들 렌더러는 사용하지 않는다 |
| 챗봇 연출 | CSS transition/Framer Motion 수준 (플로팅 버튼·바텀시트·말풍선). 리깅·스프라이트 없음 | 데모 범위. 키웅이 에셋은 플레이스홀더 시작 |
| F10·뉴스 오케스트레이션 | **TypeScript 규칙 라우터 + OpenAI SDK + 명시적 런타임 스키마 검증**. MVP에는 LangChain·LangGraph·PydanticAI 미도입 | F10은 단일 라우팅, 뉴스는 고정된 3역할 순차 흐름이면 충분. Python 서비스나 그래프 런타임을 추가할 이득보다 복잡도가 큼 |
| 배포 | Vercel (무료) | 환경변수로 키 관리. 로컬 시연 백업: `npm run dev` |

## 개발 병렬 작업 하네스

- 앱과 같은 런타임인 Node.js의 무의존성 스크립트 `scripts/git-session-manager.mjs`를 사용한다. 병렬 작업 관리 때문에 Python 서비스나 별도 데이터베이스를 추가하지 않는다.
- 루트 `main` worktree는 관제 전용으로 유지하고, 기능 작업은 레포와 나란한 `KDA육개장-worktrees/{사용AI}/{작업자}/{한글-작업명}` 아래의 전용 worktree에서 수행한다. 경로는 `git-session-manager.mjs start`가 만들어 주는 값을 그대로 쓴다.
- 세션 관리기는 브랜치 신원, worktree 경로, 예상 수정 범위 claim, 활성 로컬 세션, 열린 GitHub PR의 수정 파일 겹침을 검사한다. 상태는 공용 Git 디렉터리의 로컬 JSON에 저장하며 저장소에는 커밋하지 않는다.
- Claude는 편집 전 훅, 모든 Git 사용자는 저장소의 pre-commit·pre-push 훅, GitHub PR은 `session-policy` 워크플로로 같은 핵심 규칙을 검증한다.
- Next.js 산출물인 `web/.next`와 `web/node_modules`는 worktree끼리 공유하지 않는다. 각 worktree에서 `npm ci`를 실행하고 개발 서버 포트는 세션 관리기가 안내한 값을 사용한다.
- 이 하네스는 개발 도구이며 Next.js 번들·Vercel 배포·F10 런타임에는 포함되지 않는다.

## LLM — GPT-5.6 Luna 단일화 (2026-08-11 확정)

F9·F10·어린이 뉴스 역할 모두 **GPT-5.6 Luna**를 사용한다. 선제 도움은 고정 스크립트이며 LLM을 호출하지 않는다. 음성·무제한 일반 대화는 범위 밖이다.

**모델 스펙 [사실 — OpenAI 공식 모델 문서]**: `gpt-5.6-luna` · 입력 $0.20 / 출력 $1.20 per 1M 토큰 · 컨텍스트 1,050,000 · 최대 출력 128,000 · 지식 컷오프 2026-02-16 · Responses·Chat Completions·Batch 지원(Realtime 미지원) · 스트리밍·structured outputs·function calling·reasoning 지원.

| AI 기능 | 모델 | 호출 패턴 | 이유 |
|---|---|---|---|
| F10 챗봇 대화 답변 | **`gpt-5.6-luna`** | 사전·FAQ 미스 또는 승인된 조회 결과 설명이 필요할 때 `responses.create({ stream: true })` 최대 1회 | 에이전트 반복 호출 없이 짧은 답변을 만들고 실패 시 고정 폴백 |
| F9 성향 리포트 서술 | **`gpt-5.6-luna`** | 주 1회 `responses.create` — 데모는 사전 생성 가능 | 엔진 JSON을 근거로 서술만 생성. 유형 라벨·수치는 엔진 산출 |
| 뉴스 관련성 선별 | **`gpt-5.6-luna`** | 후보마다 비스트리밍 structured output, reasoning `high` | 오늘 시황·51개 기업의 직접 사건만 통과시키고 중심/주변 근거를 분리 |
| 어린이 뉴스 편집 | **`gpt-5.6-luna`** | 선별된 문장만 입력, reasoning `medium`; 결정적 게이트 실패 시 `high` 1회 재시도 | 중심 사건을 먼저 쓰고 남은 어려운 용어를 전부 쉬운 말로 처리 |
| 뉴스 독립 출고 검수 | **`gpt-5.6-luna`** | 원문 전체+실제 노출문으로 비스트리밍 structured output, reasoning `high` | 선별·편집자의 자체 선언을 보지 않고 주체·중요성·초점·근거·용어를 재판정 |
| 출력 필터 1단 | LLM 아님 — 정규식·키워드 룰 | 전 출력 | 검사기 자신이 환각하지 않는 결정적 하한선. 단독으로는 의미 위반을 못 막는다 |
| 출력 필터 2단 | **`gpt-5.6-luna`** | 1단을 통과한 **모델 생성 답변에만** 비스트리밍 structured output, reasoning `low` 1회 | 표현만 바꾼 추천·전망을 정규식이 못 잡는다(실측 0/12). 고정·사전·FAQ·Tool 응답은 승인 텍스트라 호출하지 않는다 |
| 행동 프로파일 엔진·정량 지표 | LLM 아님 — `shared/engine` 순수 함수 | 상시 | LLM은 수치를 만들지 않고, 행동 감지 또는 사용자 대화 때만 서버에서 호출된다 |

### 구현 규칙

- SDK: **`openai`** 서버 전용, **Responses API**를 쓴다. API 키는 `OPENAI_API_KEY`만 사용한다.
- 모델 ID는 `shared/llm`의 단일 상수 `gpt-5.6-luna`로 둔다. 별도 프로바이더 추상화는 만들지 않는다. **별칭 `gpt-5.6`은 sol로 라우팅되므로 쓰지 않는다.**
- F10은 서버에서 `client.responses.create({ stream: true })`를 호출하고 `response.output_text.delta` 이벤트만 읽는다. 원시 델타 전체를 최대 4,000자로 서버 버퍼에 먼저 모은 뒤, 생성이 끝난 전체 텍스트를 최대 3문장·출처·권한·수치·금지 표현 기준으로 공통 검증한다. 통과한 완성 답변만 단일 `text` SSE 이벤트로 보내며 차단 시 고정 응답으로 대체한다.
- F9은 `client.responses.create`(비스트리밍)로 서술만 생성하고 공통 출력 필터를 거친다.
- **출력 검사는 2단이다 [개정 2026-08-14].** 1단은 `shared/llm/filter`의 결정적 룰이고, 2단은 `f10-chatbot/lib/output-judge`의 Luna 판정이다. 2단은 `source === "model"`인 답변에만 돌리므로 고정 응답·사전·FAQ·Tool 경로에는 호출이 없다. 판정 실패·시간 초과는 닫힌 실패로 처리해 고정 응답으로 대체한다. 2단이 환각해도 1단이 잡던 표현은 그대로 차단되므로 안전 하한선은 내려가지 않는다.
- 2단 도입 근거는 실측이다. 같은 의미를 다른 표현으로 쓴 추천·전망 문장을 1단 룰은 0/12 차단했고, 룰을 8개 확장해도 표현을 바꾼 5문장은 0/5 차단이었다. Luna 판정은 12/12·5/5를 차단하고 정상 설명 8건은 0건 오탐했다. 단건 지연 약 1.5초는 SPEC §5.2의 최소 2초 답변 준비 카드 안에 들어간다.
- 수치 검증은 **단위가 붙은 주장값만** 대조한다(`123원`·`12%`·`10주`). `1주당`처럼 단위 뒤에 조사가 붙는 관용구와 단위 없는 설명용 숫자는 화면 수치 주장이 아니므로 통과시킨다. 허용 목록은 화면 맥락값과 **사용자가 같은 질문에 적은 숫자**로 구성한다.
- F9·F10 대화의 `reasoning.effort`는 **`low`** — 무대 지연을 줄이되 3문장·거절 지시 준수는 유지한다. `max_output_tokens`는 reasoning 토큰까지 포함하므로 800으로 두고, 3문장 상한은 서버 라우트가 자른다. 지연이 더 문제면 `none`으로 내린다.
- F10 대화는 텍스트 입력만 제공한다. 음성 파이프라인은 범위 밖이다(Luna는 Realtime 미지원).

### 어린이 뉴스 역할 파이프라인 프로토타입 [팀 확정 2026-08-12]

현재 구현은 `web/features/f2-trade/prototypes/child-news-role-pipeline/`에 격리한 프로토타입이다. 일일 수집 스케줄과 Supabase 저장에는 아직 연결하지 않는다.

- 범위는 `오늘 국내 시황` 또는 `선정 51개 기업의 직접적인 중요 사건` 두 가지다. 채용·행사·사회공헌·사내 혁신·일반 홍보와 선정 기업이 고객·제공 채널로만 등장하는 기사는 제외한다.
- 원문은 문장 단위 근거 id로 나눈다. 관련성 선별자가 포함한 문장만 편집자에게 전달해 코스닥 동향·개발자 강연 같은 주변 사실이 문안으로 되돌아오는 경로를 끊는다.
- 편집자는 선별자가 표시한 어려운 용어를 모두 `replaced` 또는 `explained`로 처리한다. 결정적 게이트는 근거 id, 중심 문장 위치, 원문에 없는 숫자, 공통 금지 표현, 긍정·부정 라벨, 용어 처리 누락을 검사한다.
- 독립 출고 검수자는 선별자의 설명이나 편집자의 자체 점수를 받지 않고 원문 전체와 실제 노출문만 본다. 검수 LLM은 의미 교차검증 역할이며, 게시 여부는 서버가 모든 boolean·issue·역할 간 주체/사건 일치를 확인해 결정한다. 공통 정규식 출력 필터는 별도로 유지한다.
- 편집 문제만 한 번 `high`로 재작성한다. 독립 검수에서 범위·주체·중요성이 다르다고 판정하면 즉시 탈락한다. 파싱 오류·시간 초과·판단 불일치는 모두 닫힌 실패로 처리한다.
- `ready_for_storage` 결과만 DB 저장 단계로 넘긴다. 통과 기사가 목표 개수보다 적어도 일상 기사로 보충하지 않는다.

### F10 라우팅·툴·RAG 경계 [팀 확정 2026-08-11]

- **한 번 통과하는 파이프라인:** 요청·세션 검증 → 입력 안전 게이트 → 규칙 기반 질문 목적 라우팅 → 선택적 읽기 전용 조회 → Luna 최대 1회 → 공통 출력 게이트 → 구조화 응답. 모델이 스스로 툴을 반복 선택하는 에이전트 루프는 만들지 않는다.
- **툴로 만들 것:** 승인된 종목 사실 조회, 본인 거래·질문식 기록 조회, 본인 F9 JSON 조회, 본인 아카이브 조회. 모두 서버 전용·읽기 전용이며 입력·출력 스키마와 허용 필드를 고정한다.
- **툴로 만들지 않을 것:** 용어 사전, 사용법 FAQ, 추천·위기·개인정보 고정 응답, 선제 도움 고정 발화. 단순한 것은 TypeScript 데이터와 순수 함수로 처리한다.
- **사용자 경계:** 사용자·가족 식별자는 세션에서 서버가 주입한다. 모델과 클라이언트는 조회 대상을 지정하거나 다른 가족 구성원으로 바꿀 수 없다.
- **RAG:** “매수가 뭐야?” 수준은 사전으로 처리한다. 승인된 교육 문서가 사전 규모를 넘어 의미 검색이 필요할 때만 소규모 RAG를 추가한다. 공개 웹 검색은 MVP 답변 경로에 두지 않는다.
- **후속 행동:** 관련 화면 이동은 모델이 만든 URL이 아니라 허용 목록의 `uiAction`을 반환하고 사용자 탭 후 실행한다.
- **재검토 조건:** 한 요청에서 여러 툴의 반복 호출, 중단 후 재개, 사람 승인, 복잡한 재시도·분기가 실제 요구사항이 될 때만 LangGraph 도입을 재검토한다. 현재 Next.js 단일 TypeScript 서비스에 별도 Python 런타임이 필요하지 않으므로 PydanticAI는 사용하지 않는다.
- **선제 도움:** 유형은 `switch`·`dwell`·`lossRevisit` 3종으로 고정하고 LLM을 호출하지 않는다. 횟수·시간·손실률 임계값만 [가정]으로 검증한다.

## 구조 원칙

- **레포에 실행 가능한 앱이 셋 있다.** `web/`(공식 앱 — 배포·발표 대상) · `mock-stock/`(F0·F2·F3·F11 화면과 차트를 먼저 만든 데모 Next 앱) · `behavior-demo/`(Vite 8 + Supabase, 행동 수집 실험). 새 기능은 `web/`에 넣는다.
- **[미해결] 통합 리스크**: `web`↔`mock-stock`은 Tailwind 4↔3, TypeScript 5.9↔7.0으로 갈려 있어 화면을 그대로 옮겨 붙일 수 없다. 옮길 때 스택을 `web` 기준으로 맞춘다.
- 전역 기획·디자인·기술 문서는 `docs/`, 기능 구현 명세는 코드 소유 폴더의 `SPEC.md`에 둔다.
- API 키(OpenAI, 키움)는 서버 코드에서만 — 클라이언트 번들 및 `NEXT_PUBLIC_*` 포함 금지
- LLM 프롬프트는 기능 폴더, SDK 호출·모델 상수·필터는 `shared/llm`에 둔다. F10 `app/api/chat`은 스트림 전송만 담당하며 컴포넌트에서 SDK를 직접 호출하지 않는다
- 행동 이벤트(확신도·체류·태깅)는 zustand 스토어에 적재 → `shared/engine`이 규칙 판정 — 트리거 판정은 클라이언트 규칙으로 충분, LLM 호출만 서버 경유
- **픽스처 우선**: 목업으로 화면을 먼저 완성하고 실연동(키움 시세·LLM)은 마지막 스위치. 시세 픽스처의 실제 위치는 `web/app/api/quote/fixtures.ts`다(`shared/data/fixtures/`는 비어 있다)

## 폴더 구조 (feature-sliced — 폴더가 소유권 경계, 가드 파일 선배치)

```
web/                        # 공식 앱 — 배포·발표 대상
  AGENTS.md · CLAUDE.md     # 앱 전역 규칙 (전 에이전트 자동 로드)
  next.config.ts
  app/                      # 라우팅 전용 — 오케스트레이터 소유, page.tsx는 features 재수출 1줄
    AGENTS.md · CLAUDE.md
    api/quote/[symbol]/     # 키움 시세 프록시 (+ chart/) — kiwoom.ts·fixtures.ts가 폴백 캐시 소유
    api/universe/           # iframe 프로토타입에 51종·시세를 넘기는 조회 엔드포인트 (+ data/)
    api/chat/               # F10 필터 통과 SSE 전송 전용 Route Handler
    front UI/               # 디자인 원본 에셋·목업 HTML 보관소 — 라우트가 아니다
  public/ui/                # 화면 정본 app.html + 종목 로고·폰트 에셋
  features/
    f0-home/                # ConnectedPrototype — iframe 임베드와 F10 오버레이 연결 (골든 패스 ①)
    f2-trade/               # SPEC.md: F2+F3 단일 명세 · 탐색·상세·주문·보유 (골든 패스 ②)
      prototypes/child-news-role-pipeline/ # 운영 연결 전 뉴스 3역할 파이프라인 프로토타입
    f3-reason/              # 질문식 기록 폼 — f2 SPEC을 공유하고 f2가 import
    f9-archive/             # SPEC.md: 성향·수익률·성장기록 + 프롬프트      (골든 패스 ⑥)
    f10-chatbot/            # SPEC.md: 챗봇 구현·콘텐츠·데이터·안전 가드     (골든 패스 ②·③·⑤)
      lib/                  # routing·orchestrator·tools·contracts·explain·session·bottom-sheet + 테스트
    f11-feed/               # SPEC.md: 가족 거래 피드 — 화면 코드는 아직 mock-stock에 있다
    (각 폴더에 AGENTS.md · CLAUDE.md — 소유권·필독 SPEC·완료 기준)
  shared/                   # 공용 — 오케스트레이터 소유, 기능 에이전트 수정 금지
    AGENTS.md · CLAUDE.md   # 공용 변경 규칙 포함
    ui/                     # 실제 소비자가 있는 공용 UI 5종 — 나머지는 기능 소유 또는 공용 후보 (디자인시스템 §6)
    engine/                 # proactive-help 등 순수 함수 + 테스트
    llm/                    # openai 서버 client + filter(금지 표현) + 모델 상수
    store/                  # zustand — chat-behavior-store(화면 진입·체류·주문 취소 이벤트)
    types/                  # 공용 타입 계약 (chatbot.ts)
    data/                   # stocks·sectors·chatbot-knowledge·종목/섹터 교육 데이터 + 테스트
mock-stock/                 # 데모 Next 앱 — F0·F2(차트)·F3·F11 화면 소유
behavior-demo/              # Vite + Supabase 행동 수집 실험
scripts/                    # git-session-manager.mjs — 병렬 작업 하네스
docs/ · records/ · svg/     # 전역 문서 · 실험 기록 · 디자인 에셋
```

`AGENTS.md`와 `CLAUDE.md`는 디렉터리별 한 쌍으로 같은 내용을 유지한다. 한쪽을 수정하면 같은 작업에서 짝 파일도 수정하고 완료 전 전체 쌍을 비교한다.

※ 유효한 기능 ID는 F0·F2·F3·F9·F10·F11이다(F11 가족 거래 피드는 2026-08-12 신설). 폐기된 F1·F4·F5·F6·F7·F8은 재사용하지 않는다 (`docs/기능명세.md` v10).

## 미결정 → 구현 직전 확정

1. **키움 REST API 앱키 발급** (사용자 계정 필요) + 모의투자 환경 지원 범위 확인 — 불가 시 일반 시세 API+가상 체결로 대체
2. 시드 데이터 시나리오 상세 (성향 리포트와 F10 선제 도움 순간이 일치하는 행동 스크립트)
3. F10 선제 도움 **확정 3종의 수치 임계값** (신호 유형은 확정, 횟수·시간·손실률은 [가정] — 시드 데이터와 정합)
4. 키웅이 캐릭터 에셋 (플레이스홀더로 시작, 최종 페르소나는 통합문서 v2 §16 확인)
