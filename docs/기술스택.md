# 기술 스택 (개정 2026-08-10 — 핵심기능 3축 반영)

## 결정

| 항목 | 결정 | 근거 |
|---|---|---|
| 프레임워크 | **Next.js (App Router) + TypeScript** | 단일 레포·단일 실행. 서버 코드로 LLM/시세 키 보호. Vercel 배포 → 발표 때 QR로 실기기 시연 |
| 스타일 | Tailwind CSS | 디자인 토큰을 config로 강제(`docs/디자인시스템.md`), 병렬 작업 시 CSS 충돌 없음 |
| 상태 | zustand + localStorage persist | 데모 규모에 DB 과잉. 민지↔엄마 스위처·거래 로그·확신도·펫 상태 전부 클라이언트 상태 |
| 시세 | **키움 REST API** (사용자 확정 — 키움 발표 프로젝트 브랜드 정합) | 국내주식 현재가·일봉. **[확인 필요]** 모의투자 환경 지원 범위·앱키 발급 절차 — 발급 전까지 픽스처로 개발 |
| 시세 폴백 | **필수** — 마지막 성공 응답을 캐시에 저장, API 실패·장외 시간엔 캐시 서빙 | 발표 시각이 장외이거나 행사장 네트워크 불안일 때 데모가 죽으면 안 됨 |
| 차트 | lightweight-charts 또는 recharts 라인 1개 | 종목 상세·수익률 추이에 간단 라인 차트만 |
| 펫 연출 | CSS transition/Framer Motion 수준 (표정 교체+간단 모션). 리깅·스프라이트 없음 | 데모 범위. 에셋은 플레이스홀더 시작 |
| 배포 | Vercel (무료) | 환경변수로 키 관리. 로컬 시연 백업: `npm run dev` |

## LLM — AI 기능별 배정 (2026-08-10 개정)

비용은 데모 볼륨에서 무의미(전 조합 몇백 원대). 기준은 **지연·품질·무대 리스크**. F7 선생님 폐기 + **자유 입력 없음**(펫 원칙) 결정으로 실시간 스트리밍·음성 파이프라인 요구가 사라졌다 — 대화형 경로 자체가 없다.

| AI 기능 | 모델 | 호출 패턴 | 이유 |
|---|---|---|---|
| F8 펫 리액션 문구 (되묻기 1~2문장+선택지) | **claude-sonnet-5** | 임계치 트리거 시 1회 — 저빈도 | 짧은 문구라 품질 충분. 트리거 후 1~2초 내 표시가 체감 품질이라 지연 우선 |
| F8 오늘의 순간 번역 (부모용) | **claude-opus-5** | 일 1회 배치 — 데모는 사전 생성 가능 | 부모 감성에 닿는 서술 품질이 관계 기능의 핵심. 지연 무관 |
| F8 주간 질문 생성·공개 후 정리 | **claude-opus-5** | 주 1회, 극소 | 질문 품질이 PoC-2 통과 기준("대화하고 싶어지는가 ≥70%")과 직결 |
| F9 성향 리포트 서술·유형 라벨 | **claude-opus-5** | 주 1회 배치 — 데모는 사전 생성 가능 | 발표 핵심 증거물. 품질 최우선 |
| 출력 필터 | LLM 아님 — 정규식·키워드 룰 | 전 출력 | 검사기에 LLM을 쓰면 검사기도 환각 리스크 |
| 행동 프로파일 엔진·정량 지표·펫 성장 계산 | LLM 아님 — `shared/engine` 순수 함수 | 상시 | 3층 구조 원칙: LLM은 수치를 만들지 않고, **임계치 통과 시에만 소환**된다 (펫 원안 §7) |

- SDK: `@anthropic-ai/sdk` 서버 전용. 모델명은 기능별 상수로 두고 한 줄 교체 가능하게.
- 가격 참고: opus-5 $5/$25, sonnet-5 $2/$10(인트로, 2026-08-31까지, 이후 $3/$15) /MTok.
- ※ GPT Realtime 음성 파이프라인(구 F7 검토안)은 **폐기** — 자유 입력 없음 결정으로 근거 소멸. `web/app/api/teacher/`의 실험 코드는 삭제 대기.

## 구조 원칙

- 앱은 `web/` 하위 (레포 루트·`docs/`는 기획 문서 유지)
- API 키(Anthropic, 키움)는 서버 코드에서만 — 클라이언트 번들 포함 금지
- LLM 프롬프트·생성 로직은 **각 기능 폴더의 server action**에, 공통 호출+필터는 `shared/llm`에. 컴포넌트에서 SDK 직접 호출 금지
- 행동 이벤트(확신도·체류·태깅)는 zustand 스토어에 적재 → `shared/engine`이 규칙 판정 — 트리거 판정은 클라이언트 규칙으로 충분, LLM 호출만 서버 경유
- **픽스처 우선**: `shared/data/fixtures`의 목업으로 화면 먼저 완성, 실연동(키움 시세·LLM)은 마지막 스위치

## 폴더 구조 (feature-sliced — 폴더가 소유권 경계, 가드 파일 선배치)

```
web/
  AGENTS.md · CLAUDE.md     # 앱 전역 규칙 (전 에이전트 자동 로드)
  app/                      # 라우팅 전용 — 오케스트레이터 소유, page.tsx는 features 재수출 1줄
    AGENTS.md · CLAUDE.md
    api/quote/[symbol]/     # 키움 시세 프록시+폴백 캐시 (인프라)
    api/teacher/            # [폐기] 구 F7 실험 코드 — 삭제 대기, 참조 금지
  features/
    f0-home/                # 홈: 펫 상주·순위·튜토리얼 코치마크        (골든 패스 ①)
    f2-trade/               # 탐색·종목 상세·매수/매도·보유              (골든 패스 ②)
    f3-reason/              # 질문식 기록: 근거 태깅+확신도 — f2가 import (골든 패스 ②)
    f8-pet/                 # 펫: 리액션·오늘의 순간·주간 질문·성장 + 프롬프트 (골든 패스 ③④⑤)
    f9-archive/             # 아카이브: 성향·수익률·성장기록 + 프롬프트    (골든 패스 ⑥)
    (각 폴더에 AGENTS.md · CLAUDE.md — 소유권·스펙·완료 기준)
  shared/                   # 공용 — 오케스트레이터 소유, 기능 에이전트 수정 금지
    AGENTS.md · CLAUDE.md   # 변경 요청 로그 포함
    ui/                     # Button·Card·Chip·PriceText·PetAvatar 등 (디자인시스템 §6)
    engine/                 # 행동 프로파일 엔진·정량 지표·펫 성장 순수 함수
    llm/                    # 공통 호출 client + filter(금지 표현) + 모델 상수
    store/                  # zustand (계정·거래·확신도·펫 상태)
    types/                  # 공용 타입 계약
    data/                   # stocks(종목+아이 설명)·seed(4주차·펫 Lv.2)·fixtures(목업)
```

※ 폐기된 기능 폴더: `f1-letter`·`f4-talk`·`f7-teacher` — 기능 ID와 함께 삭제, 재사용 금지 (기능명세 v4).

## 미결정 → 구현 직전 확정

1. **키움 REST API 앱키 발급** (사용자 계정 필요) + 모의투자 환경 지원 범위 확인 — 불가 시 일반 시세 API+가상 체결로 대체
2. 시드 데이터 시나리오 상세 (성향 리포트·펫 감지 순간과 일치하는 행동 스크립트, 펫 Lv.2 상태 포함)
3. 펫 트리거 임계치 수치 (확신도·체류 기준 — 시드 데이터와 정합)
4. 펫 캐릭터 에셋 (플레이스홀더로 시작, 기존 마스코트 통합 여부는 기획서 §15-7)
