# 키움 가족 모의투자 리그 — KDA 발표 프로젝트

부모·자녀가 각자 모의투자하고, AI가 행동 스코어링으로 각자의 투자성향을 읽어주는 가족형 리그. 아이 옆엔 눈높이 챗봇 "키웅이". 데모 웹앱 + 발표 자료를 만든다.

## 세션 시작 시

1. `docs/기능명세.md` 읽기 (기능 ID F0·F2·F3·F9·F10, 골든 패스)
2. 코드 작업이면 **작업 폴더의 가드 파일이 네 규칙이다** — `web/` 이하 모든 소유 경계 폴더에 `AGENTS.md`(본문)+`CLAUDE.md`(@임포트) 쌍이 있다. 전체 지도: `docs/하네스.md`
3. UI 작업이면 `docs/디자인시스템.md` 필독 (토큰 8색·컴포넌트 10종·말투 — 강제)
4. 기획 판단이 필요하면 `docs/기획서.md` v6가 유일한 진실 원천. `docs/펫서비스기획.md`는 폐기된 v5 원안 — 참조 금지(보존만)

## 핵심 규칙 요약

- 문서 우선순위: 기획서 > 기능명세 > 디자인시스템 > 기술스택·가드 파일 (충돌 시)
- 기능 에이전트는 자기 `features/f*` 폴더 안에서만 쓴다. `shared/`는 import 전용 — 변경은 `web/shared/AGENTS.md` 요청 로그 절차
- LLM 출력은 반드시 `shared/llm` 필터 경유. AI가 종목 추천·목표가·수익률 전망·훈계를 말하면 버그
- **챗봇(F10)은 개인 코치** — 기능 범위 9종(기획서 §6.4 표: 개념·사용법·종목 안내·기록 되짚기·성향 설명·아카이브 탐색 등)만. "뭐 사?"류 추천 요구는 고정 거절+기준 안내, 위기 표현은 안전 종료+보호 안내. 선제 발화는 2계층(사용법+불안)·고정 스크립트(LLM 미사용)·발화 제한·부모 미전달(명세 F10-B). 성향 스코어는 스타일이지 성적이 아니다 — 실력 점수·등급·채점 노출 금지
- [사실]/[추론]/[가정] 표기를 발췌 시 떼지 말 것
- 원안 보존 (수정 금지): `docs/키움_가족모의투자_팀공유_정리_v2.md`, `docs/펫서비스기획.md`
- md가 원본, `docs/자료정리.html`은 뷰 — 기획 변경 시 md 먼저

## 코딩 원칙 (전 폴더 공통)

코드를 쓰거나 고치거나 리뷰할 때 항상. 자명한 한 줄 수정엔 판단으로 생략한다. 전문은 `/andrej-karpathy-skills:karpathy-guidelines`.

- **먼저 생각한다** — 가정은 말로 꺼낸다. 해석이 갈리면 하나를 몰래 고르지 말고 둘 다 제시. 더 단순한 길이 보이면 말하고 밀어붙인다. 막히면 멈추고 뭐가 불명확한지 이름을 붙인다.
- **단순함 우선** — 요청 밖 기능, 1회용 코드의 추상화, 요청 없는 설정 옵션, 일어날 수 없는 케이스의 예외처리 전부 금지. 200줄이 50줄로 되면 다시 쓴다.
- **외과적 수정** — 인접 코드·주석·포맷 "개선" 금지. 안 깨진 걸 리팩터 금지. 내 취향과 달라도 기존 스타일에 맞춘다. 남의 데드코드는 지우지 말고 보고만 한다. 내 변경이 만든 미사용 import·변수만 정리한다.
- **검증 가능한 완료 기준** — "동작하게 해줘"를 검증 가능한 목표로 바꾼다. 이 프로젝트의 검증은 `npm run build` 통과 + 담당 기능의 골든 패스 스텝 재현. 다단계 작업은 착수 전에 `단계 → 검증` 한 줄씩 계획한다.

한 줄 테스트: **바뀐 모든 줄이 사용자 요청으로 곧장 추적되는가.**

## 스택 (docs/기술스택.md)

Next.js(App Router)+TS+Tailwind, `web/` 하위 feature-sliced. LLM: F10 챗봇=claude-sonnet-5(스트리밍·지연), F9 성향 서술=claude-opus-5(품질) — 전부 서버 경유. 챗봇은 용어 사전·FAQ 매칭 우선, LLM은 미스 시 폴백. 스코어링(성향 5축)은 룰 엔진 — LLM이 수치를 만들면 버그. 시세=키움 REST API+폴백 캐시. 상태=zustand. 픽스처 우선 개발.

## 명령어·위임

- 개발 `npm run dev` / 검증 `npm run build` / 배포 Vercel
- 서브에이전트: **paseo `create_agent`로만** — codex CLI 직접 호출·Claude 서브에이전트 금지 (전역 설정과 충돌 시 이 규칙이 이김). 모델은 `codex/gpt-5.6-sol|terra|luna` 3종, `thinkingOptionId`는 `medium`|`xhigh`|`max`를 사용한다. 위임 프롬프트에 담당 폴더 경로 명시 ("그 폴더의 AGENTS.md가 네 규칙") — `docs/하네스.md` §위임
- 예외: `.claude/agents/child-news-*` 2종은 어린이 뉴스 A/B 프로토타입 구현용 Claude 서브에이전트 — 스킬 경유로만, 웹앱 코드 작업엔 금지

## 하네스: 어린이 뉴스 모델 벤치마크

**목표:** 같은 뉴스 입력으로 여러 모델·추론 강도의 초등 고학년용 브리핑을 생성·검증·비교한다.

**트리거:** 기사 한 건 변환·두 모델 비교는 `child-news-compare`, 네이버 당일 뉴스 10건과 Sol·Terra·Luna 비교는 `child-news-benchmark` 스킬을 사용한다. 서브에이전트 사용을 명시적으로 허용한 요청에서만 실행한다.

**변경 이력:**

| 날짜 | 변경 내용 | 대상 | 사유 |
|---|---|---|---|
| 2026-08-10 | 초기 구성 | 전체 | API 없이 플랫폼 서브에이전트로 어린이 뉴스 A/B 프로토타입 실행 |
| 2026-08-10 | 네이버 API·5설정 벤치마크 확장 | 수집·생성·검증·리포트 | Sol/Terra medium·max와 Luna max를 동일한 10건으로 비교 |
