# 키움 가족 모의투자 리그 — 공통 규칙

부모·자녀가 각자 모의투자하고 행동 기반 투자성향을 비교하는 데모 웹앱. 자녀용 챗봇은 F10 "키웅이".

- 작업 전에는 가장 가까운 가드와 그 가드가 가리키는 기능 `SPEC.md`만 우선 읽는다. 기능 지도는 `docs/기능명세.md`, 제품 목표·법무·레드라인은 `docs/영웅키움_기획_통합문서_v2.md`, UI는 `docs/디자인시스템.md`, 인프라·API는 `docs/기술스택.md`를 관련 작업일 때만 추가로 읽는다.
- 제품 결정은 통합문서 v2, 기능 구현 상세는 해당 기능 폴더의 `SPEC.md`가 단일 원본이다. 전역 제품 결정과 충돌하면 통합문서 v2를 따른다.
- 가장 가까운 가드가 소유 범위를 정한다. 범위 밖은 수정하지 않고 공용 변경은 `web/shared/AGENTS.md`를 따른다.
- 모든 디렉터리의 `AGENTS.md`·`CLAUDE.md`는 한 쌍이며 내용이 같아야 한다. 둘 중 하나를 수정하면 같은 작업에서 짝 파일도 동일하게 수정하고 완료 전 전체 쌍을 비교한다.
- LLM은 `gpt-5.6-luna`와 서버 전용 `openai` Responses API만 사용하고 `web/shared/llm` 필터를 거친다. 종목 추천·매매 시점·목표가·수익률 전망·훈계는 금지한다.
- 구 기획 문서를 복구·참조하지 않는다. 기획 변경은 통합문서 v2부터 반영하고 [사실]/[추론]/[가정] 표기를 유지한다.
- 요청 범위만 최소 수정하고 추측하지 않는다. 코드 변경은 `web/`의 `npm run build`와 해당 골든 패스로 검증한다.

## Git 브랜치·워크트리 규칙

- 새 코드·문서 작업은 기본 작업 폴더가 아닌 작업별 전용 Git worktree에서 시작한다. 다른 사람의 worktree를 함께 쓰거나 그 안의 파일을 수정하지 않는다.
- 작업 하나는 브랜치 하나와 worktree 하나만 사용한다. 브랜치와 worktree는 작업이 끝날 때까지 1:1로 유지하며 다른 작업에 재사용하지 않는다.
- 브랜치 이름은 `{사용AI}/{작업자이름}/{작업명}`으로 정한다.
  - `사용AI`는 소문자 `claude` 또는 `codex`만 허용한다.
  - `작업자이름`은 여섯 명이 합의한 각자의 고유 이름을 사용하고, 같은 사람은 항상 같은 표기를 사용한다.
  - `작업명`은 한글 낱말만 사용하고 여러 낱말은 하이픈(`-`)으로 연결한다. 형식은 `^[가-힣]+(?:-[가-힣]+)*$`이며 영문·숫자·공백은 금지한다.
  - 예: `codex/홍길동/챗봇-안전게이트`, `claude/김영웅/주문화면-오류수정`.
- worktree 경로는 저장소 옆의 `../six-opening-worktrees/{사용AI}/{작업자이름}/{작업명}`을 사용한다. 마지막 세 경로는 브랜치의 세 구간과 정확히 같아야 한다.
- 새 작업은 `git fetch origin`으로 원격 상태를 확인한 뒤 최신 `origin/main`에서 브랜치와 worktree를 함께 만든다. 예: `git worktree add ../six-opening-worktrees/codex/홍길동/챗봇-안전게이트 -b codex/홍길동/챗봇-안전게이트 origin/main`.
- 커밋·푸시·PR 전에는 현재 브랜치와 worktree 경로가 일치하는지, 작업과 관계없는 파일이 포함되지 않았는지, 필수 빌드와 골든 패스가 통과했는지 확인한다.
- PR이 병합되고 worktree에 미커밋 변경이 없는 것을 확인한 뒤에만 worktree와 로컬 브랜치를 정리한다. 병합되지 않았거나 변경이 남은 worktree는 삭제하지 않는다.
- 이 규칙을 도입하기 전에 이미 생성되어 진행 중인 브랜치와 PR은 기존 이름을 유지한다. 다음 새 작업부터 새 규칙을 적용한다.

## 규칙 변경 이력

| 날짜 | 변경 내용 | 이유 |
|---|---|---|
| 2026-08-12 | 브랜치와 worktree 이름을 `{사용AI}/{작업자이름}/{한글-작업명}`으로 통일 | 6인 동시 작업에서 담당자와 작업 범위를 명확히 구분하기 위해 |
